# 媒体页面空状态统一（2026-09-27）

状态：已实施。

## 产品决策

复用已有 Empty 基元组合 `MediaEmptyState`；不新增基础组件体系。无内容统一「目前这里空空如也」，保留一句和当前页面有关的用户提示。搜索/筛选结果为空独立表达，不误导用户重新导入；排序不改变空状态类型。加载和请求失败不显示集合为空。

范围包括影片及收藏/最近加入/回收站、演员列表及演员影片、漫画、写真、愿望单、首页、萃取帧和观看历史。消息列表、设置配置列表、详情页图片缺失及数据洞察不属于本次媒体集合空状态。

采用居中图标/标题/短提示、统一边框/间距/底色；已有清除筛选、前往资料库等动作保留，移动端按钮至少 44px。移除文件格式、扫描、Mock 等技术说明，中文/英文/日文同步。

## 行为边界与验证

愿望单默认待入库分类为空时额外读取一次全状态计数；若仍有其他状态的影片，显示筛选无结果并可查看全部。计数只读取，不修改任何内容。

组件与页面回归覆盖正常列表、真正为空、搜索/筛选为空、清除动作、加载/失败状态，以及排序不会被误判为筛选。浏览器检查使用本机现有空库与搜索路由，保留真实数据。沿用原布局令牌，未运行跨浏览器显示缩放套件。
