# Desktop / Server 拆分、连接管理与 SSDP：需求及实施计划

状态：产品边界已讨论明确；组件版本、远程权限与服务器列表/切换已按后续章节实施，SSDP、独立三包与完整生命周期拆分仍待实施。日期：2026-09-25。第 1–9 节记录需求与设计边界，第 10 节为可执行的分阶段实施计划。

## 1. 需求与当前事实

### 2026-09-26 关于页版本与包名核对

- 当前关于页展示 Server 版本及安装包版本，没有独立 Desktop 版本行。Electron 的 `app.getVersion()` 已用于请求头和入口查询参数，但没有通过可信桌面桥接提供给关于页；开发仓库版本为 `0.0.1-master`。
- 当前更新检查由 Go `internal/appupdate` 执行，以 `version.PackageVersion()` 比较 GitHub 最新发布。开发态回退值为 `0.0.0`；该值不是本机 Desktop 版本。不能把现有「安装包版本」直接改名为「Desktop 版本」。
- 现有资产解析只选择 EXE，并优先名称含 setup/installer 的资产，没有按 Desktop / Server、客户端平台和架构独立匹配。因此 macOS Desktop 不能把现有后端检查结果当作自身更新；没有匹配产物时应明确说明未提供更新包。
- 已落地的 Windows 一体安装包名为 `Curated-Setup-<version>.exe`，便携包为 `Curated-<version>-windows-x64.zip`。当前发布版本文件为 `1.5.7`。打包时 Electron 的 `curated-desktop` package 版本被写成同批发布版本；它是内部包标识，不是独立 Desktop 安装包已实现的证据。
- 首次核对时，第 10 节的三包名称仍是建议。后续命名规范已在第 11 节落盘；独立 Desktop 更新器、三包构建和 macOS 安装链仍未实现。

用户希望桌面端能够独立连接其他机器上的 Curated 服务端：首次未连接时填写地址和端口，之后能够更换服务器，并增加 SSDP 自动发现。本轮仅讨论需求。

代码核对：

- `electron/main.ts` 先等待 `startBackend()`，成功后才创建主窗口；启动失败会退出，因此目前没有独立于后端的连接入口。
- `electron/backend-process.ts` 支持 `CURATED_ELECTRON_BACKEND_URL` / `CURATED_BACKEND_URL`，生产默认 `http://127.0.0.1:8081`。健康探测失败后仍会启动本机 Go 进程，这条流程不适合直接承担远程连接模式。
- 生产桌面窗口加载后端托管的 Web UI，业务 API 默认同源；`src/api/http-client.ts` 的显式地址来自构建环境，尚不是运行时连接管理。
- Electron preload 当前暴露本机目录选择。服务端位于另一台机器时，本机目录路径不等于服务端可访问的目录。
- 当前存在 LAN 开关和 PIN 会话体系；SSDP 与用户可操作的服务器切换尚未发现实现。

## 2. 已明确的产品模型

用户已明确：Desktop 不再内置本机 Server；提供 Desktop + Server 完整包、仅 Server 包、仅 Desktop 包三种安装包。常规 Server 安装体验通过完整包默认安装 Desktop，专用纯 Server 包只安装服务端。桌面端负责连接与交互，服务端持有数据库、媒体目录、扫描与转码任务。本机与远程仅是连接目标不同：

| 模式 | 桌面端行为 | 服务端生命周期 |
|---|---|---|
| 连接本机 Server | 连接已经独立安装和运行的本机服务 | Server 独立运行，不随 Desktop 退出而停止 |
| 连接远程 Server | 手动地址或发现列表选择 | Server 独立运行；连接失败不启动本机后端 |

Desktop 作为独立客户端发行，不携带 Go 服务端、服务端数据库或 FFmpeg 转码运行时。Server 发行物包含后端与 Web UI；完整包组合 Server 和 Desktop 两个独立组件，默认同时安装。服务端独立部署衔接既有容器部署规划，NAS/无桌面容器使用纯 Server 形态，不要求安装 Desktop。

首次启动展示本地连接页，包含已知本机 Server（如有）、局域网发现结果及手动连接。没有本机 Server 时仍正常进入连接页，不自动安装或启动服务端。手动入口接受主机/IP 与端口，并支持完整 HTTP/HTTPS URL；生产默认端口可提示 8081，不硬编码为唯一端口。反向代理子路径支持需要单独决定，当前 URL 归一化会丢弃路径。

选择服务器后依次检查可达性、Curated 身份、版本/能力及认证要求；需要 PIN 时进入已有解锁流程。成功后记住连接。发现一台服务器也不自动选择或自动信任。

## 3. 连接管理与异常流程

- 设置内提供“当前服务器”和“更换服务器”；桌面托盘或应用菜单也提供入口，保证远程页面无法加载时仍可操作。
- 下次启动自动尝试上次连接；连接中即可取消或更换，超时/断线展示重试与更换入口，不出现空白页或直接退出。
- 不自动回退到本机空资料库，以免用户误以为远程数据丢失。
- 第一版可保存最近连接，一次仅连接一个服务器；不做资料库聚合与多服务器同时工作。
- 切换时停止旧页面请求、SSE、播放器与上传等客户端活动，隔离新旧服务器缓存及会话。前台上传/播放需明确提示影响；已经提交的服务端扫描等后台任务继续执行。
- 连接配置保存于桌面端用户数据目录，不能放在远程 `library-config.cfg`，否则未连接时无法读取。认证会话与普通地址配置分开存放，并按服务器隔离；端口不同也可能共享浏览器 Cookie 主机范围，不能仅依赖 origin 默认隔离。
- 服务端稳定 ID 用于去重和识别地址变化，但 SSDP 声明的 ID 不是认证凭据；地址变化后重新验证，不向未经确认的候选端点转发旧凭据。

## 4. SSDP 的职责与边界

SSDP 仅用于发现可连接的 Curated 服务，业务流量继续使用 HTTP/HTTPS。建议由 Go 服务端发布和响应发现，由 Electron 主进程发送查询并收集结果；普通浏览器不直接执行 UDP 发现。

协议草案：IPv4 SSDP 使用 `239.255.255.250:1900/UDP`，定义 Curated 专属服务类型与稳定 UUID；客户端发送 M-SEARCH，服务端返回 USN、LOCATION、缓存有效期等标准信息，通过 HTTP 描述取得名称、版本与连接端点。服务端可发送 alive/byebye；UI 不应依赖 byebye 一定送达，应按有效期过期并支持刷新。

- 按稳定 ID 合并多网卡结果，但保留可达地址候选；公告地址必须是客户端可访问地址，不能是 `127.0.0.1`、`0.0.0.0` 或容器内部地址。
- 局域网发现随服务端对外访问开启，建议另提供发现开关。只开放 loopback 的实例不向局域网公告不可达服务。
- 发现信息限名称、ID、版本和连接信息，不携带媒体列表、目录、PIN 或会话。
- 将发现响应视为未验证输入，限制描述请求的协议、重定向、响应大小和超时；点击连接后再执行身份/能力验证。
- SSDP 通常只覆盖同一局域网多播范围；跨 VLAN、访客 Wi-Fi、VPN 与 Docker bridge 不保证可发现。NAS/容器部署需明确多播网络与 HTTP 公告地址；手动地址始终可用。
- 发现失败不等于服务器离线，不能阻止已保存或手工地址连接。SSDP 不承担公网穿透，也不意味着增加 DLNA 媒体服务。

## 5. 当前选定的架构方向

用户明确倾向“Desktop 连接后加载服务器提供的 Web 界面”，后续讨论以“本地连接页 + 服务端 Web UI”为当前方向，尚未实施。这与现有生产架构接近，业务 API、图片、视频、SSE 继续同源，可以减少客户端与服务端前端版本错配。

本地连接页及更换入口必须由安装包提供，不能依赖任何服务端响应。将其拥有的连接管理 IPC 与远程内容可用的桥接能力隔离，校验调用来源与导航/重定向。远程网页不应获得任意本机文件或进程能力；不能为跨域关闭 Electron 安全机制。服务端版本与桌面桥接版本仍需能力协商。

Desktop 随包提供连接、发现、连接失败与更换服务器等本地界面；资料库业务界面由当前 Server 提供。不随 Desktop 另行打包完整业务前端作为本期方向。

Desktop 在加载页面失败、网络中断或服务器重启时保留本地连接管理入口；短暂断线先显示连接状态，避免立即销毁尚有未提交输入的页面。提供重试和返回连接页，不承诺离线浏览远端资料库。实际故障展示方式仍待交互设计。

## 6. 必须定义的操作归属

| 操作 | 远程模式下的归属 |
|---|---|
| 添加媒体库 | 服务端文件系统；仅服务器本机配置，远程端只读（已按 §14 实施） |
| 备份输出目录 | 服务端文件系统；远程配置行为不在 §14 本次修改范围，仍待梳理 |
| 上传本机文件 | 客户端选择文件，经 HTTP 上传 |
| 扫描、刮削、转码、整理 | 服务端执行 |
| 打开文件所在目录 | 明确服务器目录与本机目录区别；远程模式不可把服务器路径交给本机文件管理器 |
| 外部播放器 | 使用客户端可访问的媒体 URL，并明确认证方式，不传远端磁盘路径 |
| 更新、开机启动、退出 | 分开定义桌面端与服务端行为，不能沿用同机假设 |

现有 PIN 可作为第一版认证起点，但发现成功不代表已授权。若目标包含公网，需要另外确定 HTTPS、访问控制及部署要求；不将已有 LAN/PIN 直接描述为完整公网产品能力。

## 7. 建议切片与验收

执行顺序以第 10 节为准：先明确组件和合约，完成 Server 独立运行与 Desktop 手动连接，补齐远程操作边界，再加入 SSDP，最后完成三种安装包、升级迁移与发布验收。组件划分先定，三包交付在运行链路验证后完成。

关键验收：Desktop 包不含服务端运行时；无本机后端也能显示连接页；远端不可达不会启动本机后端；切换 A/B 不串数据和认证；重启记住上次连接；多台服务器可选择；禁用多播仍能手动连接；PIN 锁定不影响更换入口；旧服务端缺少发现能力仍有明确的手动连接兼容策略；退出客户端不停止本机或远端 Server 任务；添加库目录始终指向正确机器；组合安装可正常连接本机 Server，且不覆盖已有 Desktop 的连接偏好。

## 8. 待讨论的范围

- “服务端不在本机”第一阶段主要是同一局域网 NAS/另一台电脑，还是也包含异地互联网连接？这决定发现的覆盖预期和远程访问要求。
- Desktop 加载服务端 Web UI、不内置 Server，三种安装包及完整包默认安装 Desktop 已明确；Server 的后台运行方式、各平台的产物形式及旧一体包升级迁移细节仍待设计。

## 9. 补充讨论：产品命名与发行边界

用户提出：拆分之后需要考虑 Web 端与桌面端的产品关系，服务端可能命名为 servers 或 core，桌面端命名为 desktop。当前已明确界面交付及安装组合方向；2026-09-26 起发布规范采用下列对外名称，文件命名见第 11 节。

### 9.1 同一品牌下的两个发行产品与一个浏览器入口

| 发行名称 | 定位 | 交付内容 |
|---|---|---|
| Curated Server | 资料库服务端，部署在 NAS、服务器或本机 | Go 服务、数据库与媒体管理、API、SSDP 发布，以及配套 Web UI |
| Curated Desktop | 安装在用户电脑上的桌面客户端 | 本地连接管理、SSDP 搜索、桌面集成，连接后加载 Server 的资料库界面 |
| Curated Web | 浏览器访问入口 | 随 Server 提供的 Web 界面，初期无需第三套独立安装产品 |

推荐对外使用单数 Server：表示可安装的服务端产品；Servers 更适合作为界面中的服务器列表名称。Core 更适合未来确有必要抽取的内部业务核心模块，对普通用户而言不如 Server 直接。无需为命名提前重排源码目录或新增空的 Core 层。

Web 前端与 Server 不是同一个技术组件，但作为同一发行物交付。Desktop 和浏览器加载同一份服务端业务界面；该界面通过能力检测适配桌面功能，普通浏览器正常工作，无需维护两套业务页面。本地连接页可以复用设计组件，但加载与运行不依赖 Server。

### 9.2 产品分离后的职责

- Server 持有资料库配置、数据库、扫描与转码任务、访问策略；Desktop 持有连接记录和本机偏好。
- Server 独立运行，本机和远程遵循相同生命周期边界，Desktop 不负责拉起或结束 Server 进程。Server 的启动、停止和开机运行由自身的安装及运行机制负责，具体机制待设计。
- Desktop 是连接已有 Server 的客户端，不内置 Server。“本机一体使用”由完整包默认同时安装 Server 和 Desktop 提供，安装组合不改变独立运行关系。
- 设置区分“此设备”和“服务器”；更新入口分别显示桌面版本和服务器版本，更新 Desktop 不隐式更新远端 Server。
- Server 与 Desktop 使用独立版本线，不再要求初期同步版本号；但连接兼容性由明确的 API/桥接协议与能力范围决定，不能永久要求产品版本号完全一致。

### 9.3 对界面交付方式的影响

用户倾向已明确，采用第 5 节的服务端托管 UI 方向：Desktop 独立发布连接页、Electron 运行时和桌面能力，资料库业务 UI 随 Server 更新。更新 Server 后，浏览器和 Desktop 在重新加载界面时获得配套版本；涉及长期打开页面与旧资源缓存时需给出刷新提示，不强制丢弃未保存输入。

业务功能更新通常只需更新 Server；连接管理、SSDP、本机集成或 Electron 运行时的变化才需要更新 Desktop。仍需维护 Server Web UI 与 Desktop 桥接协议的兼容：可选桌面能力缺失时隐藏或降级相关功能；确有必需能力不兼容时明确提示所需 Desktop 版本，保留连接管理入口。版本不必完全一致。

产品名称确定为 Curated Server / Curated Desktop / Curated Web；远程网络覆盖范围仍待确认。界面交付方式与安装组合方向不再列为未决选项。

### 9.4 三种安装包与默认安装体验

用户已明确以下三种可分别下载的安装包，完整包安装 Server 时默认安装 Desktop：

| 安装包 | 组件内容 | 默认安装行为 | 典型用途 |
|---|---|---|---|
| 完整包（Desktop + Server） | Server、Web UI、Desktop | 同时安装 Server 与 Desktop | 单机用户、本机管理资料库 |
| 纯 Server 包 | Server、Web UI 及服务端运行依赖 | 仅安装 Server，不附带或自动下载 Desktop | 专用服务器、浏览器访问、NAS 部署 |
| 纯 Desktop 包 | Desktop 与本地连接管理界面 | 仅安装 Desktop，不附带或自动安装 Server | 连接本机已部署服务或远程服务器 |

建议下载页将完整包作为有桌面环境的普通用户默认推荐，明确标注三个包的组件，避免把“Server 默认安装 Desktop”错误应用于纯 Server 包。完整包若提供组件选择，“安装 Desktop”默认勾选；是否允许取消可在安装器交互阶段确定，不影响三个独立包的要求。

三种包是两个组件的不同组合，不形成三套业务实现。完整包复用与独立包一致的 Server / Desktop 构建产物，并记录各组件版本。容器/NAS 发行物保留纯 Server 形态；安装文件命名以第 11 节为准；具体平台支持仍待安装链实现，不表示每个平台均需生成三种图形安装器。

建议组合安装在新装 Desktop 且无连接记录时提供本机实际服务地址，作为明确的连接候选；启动后仍执行身份、可达性及认证检查。此本机入口不依赖 SSDP 多播，服务端只监听 loopback 时也应可用。若已有 Desktop，保留上次服务器与用户偏好，不强制改连本机。

两个组件分别管理更新、安装检测和卸载。完整包安装器复用受支持的 Desktop 发行物；检测到已安装 Desktop 时避免无条件覆盖或降级。卸载 Desktop 不删除 Server 和资料库；卸载 Server 后 Desktop 仍可连接其他服务器。Server 数据保留与删除按独立、明确的卸载策略处理。

安装验收增加：完整包在新机器默认装好两个组件；纯 Server 包不安装 Desktop；纯 Desktop 包不安装 Server；先装任一独立组件后再运行完整包不会创建重复实例、降级已有组件或覆盖连接记录；组合安装后退出 Desktop 不停止 Server。

旧版一体包迁移需要单独设计：保留数据库、媒体路径、配置、会话及现有连接体验，调整快捷方式与进程管理，不在本轮直接实施迁移或重命名。

本次仅维护需求讨论文档，没有修改业务代码、运行配置或已实现架构说明，也没有执行构建与测试。

## 10. 实施计划（待审阅）

### 10.1 本轮实施目标与范围

已确定的交付目标：

1. Desktop 不携带、不启动、不停止 Server；同机与远程使用同一连接模型。
2. Desktop 自带首次连接、手动地址、服务器发现、切换及失败恢复界面，连接后加载 Server Web UI。
3. Server 独立运行，提供 Web、API 和 SSDP 公告；Desktop 搜索并由用户选择连接。
4. 产出 Full（Server + Desktop，默认同时安装）、Server-only、Desktop-only 三种可独立下载的安装包。
5. 两组件独立设置、更新与卸载，保留旧一体包用户的数据与正常使用路径。

建议首期发布范围：沿用现有 Windows x64 安装链，覆盖同机和同一局域网。协议接受 HTTP/HTTPS 与自定义端口；公网部署、自动 HTTPS、穿透、跨网段自动发现、完整 NAS/FPK 发布和新增 macOS/Linux Desktop 安装链不纳入本轮交付。这是用于控制实施范围的建议，不是用户已经确认的限制。代码保持跨平台边界，不把 Windows 安装细节放入业务层。

继续使用当前仓库、Vue 业务前端及 Go 分层，不因产品命名立即拆仓库或大规模重排目录。完整包不会产生第三套业务实现。

### 10.2 代码核对发现与对应改动

| 现状 | 实施动作 |
|---|---|
| `electron/main.ts` 必须先启动/探测后端才能建窗口，失败退出 | 改成先开本地连接页，再建立连接；退出仅清理 Desktop 自有资源 |
| `electron/backend-process.ts` 探测失败会 spawn Go 进程 | 从生产 Desktop 移除这条路径；健康探测抽为纯连接能力，开发编排另放脚本 |
| `pnpm dev:electron` / `build:electron` 包含后端构建 | 拆开客户端开发与全栈开发入口，客户端构建不依赖 Go 或 FFmpeg |
| `backend/cmd/curated` 已有 `http` / `tray` 模式 | 复用并整理为独立 Server 启动入口，避免重写服务运行层 |
| release 数据目录默认在当前用户目录 | 首期保持运行账户和数据根，避免误读空库；服务账户转换不混入默认迁移 |
| `/api/health` 当前包含数据库路径 | 不用于 SSDP 描述；新建最小公开身份端点，另评估并迁移 health 中不必要的路径暴露 |
| preload 仅提供本机目录选择，未区分本机/远端库路径 | 拆分本地连接管理桥与业务页桥，按操作归属提供能力 |
| `assemble_release()` 将 Go、Web、FFmpeg 放入 Electron 的 `resources/app` | 拆成 Server 和 Desktop 两个组件目录，再组合为三种发行产物 |
| `Curated.iss.tpl` 只有一个安装身份和整包复制逻辑 | 建立独立组件身份与安装目录，完整包协调两者安装 |
| `appupdate.resolveInstallerAsset()` 优先选择含 setup/installer 的 EXE | 更新必须按组件、平台、架构精确选择，禁止继续猜测首个安装程序 |

### 10.3 阶段 0：冻结组件边界和连接合约

**产出：合约、配置归属、连接状态及安装身份设计。** 该阶段先完成评审性设计与最小合约切片，不修改资料库主流程。

- 约定组件标识 `server` / `desktop`，包类型 `full` / `server` / `desktop`；展示名称暂按 Curated Server / Curated Desktop。
- 拟新增 `GET /api/server-info`：返回产品标识、稳定 `serverId`、展示名称、Server 版本、连接协议版本和必要能力摘要；未解锁也可读，但不含数据库路径、资料库内容、PIN 或令牌。具体 DTO 和错误码先定义在现有 contracts 层。
- 稳定 ID 存于 Server 数据根，不能每次重启生成。备份恢复/克隆到另一独立实例时需有重新生成身份的规则，避免两台机器被发现列表错误合并；ID 用于识别，不代替认证。
- Desktop 本地配置保存地址、上次连接与非敏感偏好，认证 Cookie 存在每个连接档案独立的 Electron session partition；同主机不同端口也隔离，禁止把凭据放入 URL 或普通配置 JSON。
- 地址首期接受 HTTP/HTTPS、主机/IP、端口，默认根路径部署；不支持的反向代理子路径明确提示，不能静默截断。规范化、DNS/连接超时、证书错误与重定向策略统一处理，不忽略 TLS 错误。
- 定义状态：未配置 → 探测中 → 加载界面 → 已连接；需要认证由 Server Web 锁屏处理；失败/不兼容/断线均保留本地连接入口。
- 旧 Server 缺少 server-info 时，仅在既有 health 明确识别为 Curated 后提供受限手动连接；标注旧版、禁用不支持的发现/桥接能力，不把任意 HTTP 页面当作 Curated。

**验收：** 合约不依赖具体地址或产品版本号相等；能区分不可达、非 Curated、旧版、证书错误与能力不兼容。

### 10.4 阶段 1：Server 独立运行

**主要落点：** `backend/cmd/curated/`、`backend/internal/desktop/`、`backend/internal/config/`、版本身份及服务端资源定位。

1. Server 自身提供启动、退出和日志入口，HTTP、静态 Web、数据库和转码资源不依赖 Electron 的目录或环境变量。
2. 建议首期 Windows 复用已有 Go 托盘及当前用户登录启动机制，托盘明确显示“Curated Server”；关闭 Desktop 不影响 Server。纯 HTTP 模式继续可供无桌面运行环境管理。
3. “登录后自动运行”与“用户未登录也运行的系统服务”明确区分。首期不默认改成 LocalSystem 服务，避免改变 SQLite 目录、网络盘凭据及媒体访问权限；若需要无人登录常驻，应先补充服务账户、数据目录和权限设计，再调整阶段工作量。
4. Desktop 的开机启动属于本机客户端偏好；Server 原有 `launchAtLogin` 继续归 Server，拆清 UI 标签和持久化位置。
5. 保留默认 loopback 与显式 LAN 开关。独立包安装不自动开放局域网；Server Web 网络设置仍可通过浏览器配置。自定义监听端口要用于后续安装连接提示。
6. 保留单实例、数据锁、正常退出和后台任务恢复机制；端口冲突时给出错误，不启动第二份同库实例。

**验收：** 不安装 Electron 也能运行 Server，并通过浏览器完成浏览、播放与任务操作；停止/启动 Desktop 不改变 Server PID 或任务状态；旧数据根读取正常。

### 10.5 阶段 2：Desktop 本地连接管理与手动连接

**主要落点：** `electron/main.ts`、`desktop-shell.ts`、`preload.cjs`；建议新增连接配置/探测/状态模块与独立本地连接页构建入口，复用现有 UI 组件与样式令牌。

1. 启动立即显示随包本地页，不等待 Go 或网络；手动地址/端口、最近服务器、重试、更换入口可离线使用。
2. 连接成功后加载目标 Server Web UI，业务 API、海报、媒体、SSE 继续同源；不为这项功能全面改写 `http-client.ts` 为跨域代理。
3. 移除生产 Desktop 的后端 spawn/kill 与 Go/FFmpeg 构建依赖。全栈开发可由显式开发脚本启动多个独立进程，不能回流进客户端生产启动逻辑。
4. 记住上次连接；首次成功后再持久化有效目标。保留可取消的连接与有限重试，失败不退出、不创建本机空库。
5. 切换服务器前提示正在播放/上传或有未保存输入的影响；切换后销毁旧业务页面及请求上下文，隔离 Cookie、缓存和 localStorage。已提交的服务端任务继续执行。
6. 本地菜单/托盘的更换入口始终可用，不能只放在远端设置页。短暂断线保留页面与本地恢复入口，重试不创建重复窗口或事件监听。
7. 对本地页与远程业务页使用不同的权限边界，校验 IPC sender、顶层 frame 和当前授权来源；保持 sandbox、contextIsolation、禁用 Node integration。跨来源导航不自动继承桌面桥接权限。

**验收：** 在没有 Go、Server 或开发工具的机器上 Desktop 能启动；手动连接 A/B、重启记忆、同主机不同端口隔离、锁屏时更换服务器、断网恢复都通过真实 Electron 验证。

### 10.6 阶段 3：远程模式的设置与操作归属

**主要落点：** 设置页面、`src/lib/pick-directory.ts`、服务契约和运行能力摘要、更新入口。

- 资料库路径仅在 Server 本机配置，远程模式只读，不调用本机目录选择器（已按 §14 实施）。备份路径仍需单独梳理，确保输入的是 Server 可访问的绝对路径；不属于 §14 本次修改范围。
- 上传仍通过本机文件选择和 HTTP 上传。梳理播放器、媒体链接、SSE、Agent 流、导出与下载是否存在硬编码 localhost 或本机路径假设。
- “打开文件位置”与外部播放器按能力隐藏或使用明确支持的方式；本轮若无安全可用的媒体 URL 认证方案，则不承诺远程外部播放器，内置 Web 播放必须可用。
- 设置/关于页区分 Server 版本与 Desktop 版本、“服务器设置”与“此设备设置”。普通浏览器不显示仅 Desktop 可用的控制。
- 桌面桥接协议做版本/能力探测；可选能力缺失可降级，不能让业务页面整体打不开。

**验收：** 两台机器有不同磁盘路径时，添加库、备份和上传不会操作错机器；媒体/HLS、SSE 与认证不残留本机地址假设；浏览器体验保持正常。

### 10.7 阶段 4：SSDP 发现闭环

**主要落点：** 建议新增 `backend/internal/discovery/` 与 `electron/discovery.ts`；Server 配置、设置网络页及本地连接页。

1. 定义 Curated 服务类型、USN、LOCATION 与设备描述格式，使用标准 SSDP 报文；拟由 `GET /discovery/description.xml` 提供最小设备描述，实际命名在合约切片定稿。
2. Go 在可供 LAN 访问且发现开关开启时监听 M-SEARCH、发送 alive/byebye；HTTP 服务就绪后公告，退出时关闭 socket。发现开关状态与 LAN 访问关联，但关闭发现不关闭手动 HTTP 连接。
3. Electron 主进程在进入发现页或点击刷新时执行有界扫描；结果展示名称、地址和版本。第一次发现不自动连接，不携带旧 Cookie 探测候选服务器。
4. 按 ID 合并多网卡响应并保留候选地址，处理超时、缓存过期、重复报文、地址变化与名称相同的不同服务器。公告不能使用 loopback、通配监听地址或容器内部地址。
5. 首期实现 IPv4 局域网发现；普通浏览器不调用 UDP。多播被拦截、Docker bridge 或跨网段失败时显示未发现并保留手动连接，不误报 HTTP 服务离线。
6. 描述请求限制协议、大小、超时及重定向；发现信息不执行为代码。配合 LAN 设置说明实际 HTTP 端口和 UDP 1900 的防火墙需求，不全局关闭防火墙。

**验收：** 两台真实机器可发现并连接；多网卡不重复成多个服务器；两台同名 Server 可区分；关闭发现或阻断多播后手动连接仍正常；服务重启与地址变化不会串凭据。单元测试采用假时钟/报文夹具，CI 不依赖实际组播环境。

### 10.8 阶段 5：三种安装包与组件更新

**主要落点：** `scripts/release/release_cli.py`、`release_lib/build_steps.py`、`models.py`、Inno Setup 模板、版本/manifest、`backend/internal/appupdate/` 和 Desktop 本地更新入口。

**构建与安装：**

- CLI 拟增加 `--variant full|server|desktop`；分别读取组件版本，按第 11 节命名。一次组件发布分配一次版本并复用到各平台和包格式；组装 Full 不递增或重写组件版本。Full 使用单独的分发版本，manifest 必须记录真实组件版本。
- Server 组件包含 Go、Web UI、FFmpeg 与必需资源；Desktop 组件包含 Electron、本地连接页与桥接资源。建立包内容清单和自动断言，防止纯 Desktop 带进 Go/FFmpeg、纯 Server 带进 Electron。
- 产物名称按第 11 节执行；新旧 feed 的迁移隔离仍是发布前置条件，不能仅重命名当前一体产物来冒充纯组件包。
- 两组件各自固定安装身份、安装目录及卸载入口；推荐完整包作为离线组合安装器调用同版本独立安装器，避免三个不同安装身份重复写同一组文件。验证提权后仍保留原用户上下文，用户数据不能落入另一管理员账户。
- 完整包默认安装两者，检查已有版本，不无条件降级；部分安装失败明确报告已装/失败组件，重试幂等。不得为回滚本次失败删除用户原有组件。
- Server 安装启动就绪后向新装 Desktop 提供实际本机地址，不覆盖已有连接；桌面快捷方式与 Server 管理入口区分。已出产的 installer/portable 包保留，新增产物不以清理旧包为前提。

**更新：**

- manifest 为每个资产明确 `component/variant/platform/arch/version/url/sha256`。Server 更新只取 server 包；Desktop 在本机连接管理界面提供自身更新/下载入口，不依赖远程 Server 可用。
- 远端 Web 页面中的“更新服务器”仅操作 Server；客户端更新通过可信本地流程执行，不接受远程网页传入任意安装 URL/路径。组件更新不自动触发另一组件更新。
- 保留现有校验和验证与下载状态；缺少匹配资产时明确不支持，禁止回退到第一个 EXE。第一版 Desktop 可先采用本地入口打开精确匹配的官方下载，不要求立即实现静默安装。
- **旧更新器为发布阻断项：** 老版本不知道包类型且会选择首个 setup EXE。不能仅升级新版解析器就把三包放入所有旧客户端可见的同一 Release。应先验证旧客户端实际可见的 feed，再选择“旧 feed 保留唯一兼容完整包，新组件 feed 承载三包”或等效隔离方案；桥接版本可引入新 feed，但不能假设所有旧用户都会先安装桥接版本。未证明旧客户端不会选错包之前不公开切换发布结构。

**验收：** 三包分别在干净 Windows 环境安装；混合已有组件/高低版本时无重复、覆盖或降级；Server 更新不下载 Desktop 包；Desktop 更新不在远端执行；安装失败可恢复；完整包与独立包使用同一组件身份和产物。

### 10.9 阶段 6：旧一体包迁移与发布验证

1. 检测旧安装身份及 `resources/app` 布局、运行进程、自动启动项、数据根和自定义配置。迁移前展示目标组件与实际资料库位置，停止旧托管链并等待数据锁释放。
2. 首选保留现有数据目录，备份配置/数据库后迁移程序布局与快捷方式；禁止将 Server 移到另一运行账户后悄悄创建空库。自定义 `CURATED_DATA_DIR`、媒体路径及旧 Cookie/localStorage 的兼容边界均需测试。
3. Desktop 新连接档案优先指向原本机端口。已有认证会话可安全迁移则保留，否则明确要求重新解锁，不能把旧令牌无条件复制到新发现地址。
4. 撤销旧 Electron 托管和旧启动路径，避免双 Server、双自启；拆分旧卸载登记时不得触发删除新组件或资料库。
5. 回滚区分程序和数据：保留旧安装包/配置备份；如数据库结构已升级，禁止直接用旧程序打开新库，恢复按现有离线备份流程执行。
6. 完成 release notes、三包下载说明、版本兼容说明和首次迁移操作手册。先在测试数据及内测机器验证，不将主资料库作为首次迁移试验。

**验收矩阵：**

| 场景 | 通过标准 |
|---|---|
| 干净机器装 Desktop | 本地连接页可用，没有 Server 进程或服务端依赖 |
| 干净机器装 Server | 浏览器可用，没有 Electron，Server 自身可管理 |
| 干净机器装完整包 | 默认两个组件，本地连接顺畅，退出 Desktop 后任务继续 |
| Desktop → 远程 Server | 认证、浏览、播放、上传、任务、切换及断线恢复正常 |
| 浏览器 → 同一 Server | 既有业务能力无回归，不要求 Desktop |
| A/B 同主机不同端口 | Cookie、缓存、偏好及异步结果不串库 |
| 发现不可用/旧 Server | 手动连接或明确兼容提示，连接页不被阻塞 |
| 单独包 → 完整包/组件升级 | 组件被复用，无重复注册、降级和连接偏好丢失 |
| 旧一体包升级 | 原库与设置可用，无双进程和双自启，认证变化有明确提示 |
| 卸载其中一组件 | 另一组件和 Server 数据不被误删 |
| 旧更新客户端看新发布 | 不会自动下载错误组件安装包 |

### 10.10 依赖顺序、检查与交付节点

执行顺序：**阶段 0 → 1 → 2 → 3 → 4 → 5 → 6**。安装身份与旧更新 feed 的设计在阶段 0 提前摸清，阶段 5 实施，不等到发包时才发现无法迁移。

| 可审阅节点 | 用户可以验证什么 | 发布状态 |
|---|---|---|
| M1：阶段 0–2 | Server 独立运行，Desktop 无后端启动依赖，可手动连接并切换 | 开发验证，不作完整远程版交付 |
| M2：阶段 3–4 | 远程目录/设置归属正确，SSDP 发现及手动兜底可用 | 功能内测 |
| M3：阶段 5–6 | 三种安装包、组件更新、旧版迁移及卸载全部通过 | 满足发布条件，实际发布另按用户指令 |

各行为切片独立提交：合约 → Server 运行 → Desktop 连接/隔离 → 远程操作边界 → SSDP → 组件构建 → 安装器 → 更新路由 → 迁移。避免一次提交全部改动；不自动 push。

验证沿用 `docs/ops/2026-04-08-agent-build-and-test.md`：开发中运行对应 Electron/Vitest/Go 包测试；阶段集成完成后执行根目录 `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm test:electron`、`pnpm test:e2e`，`backend/` 下 `go test ./...` 与 `go vet ./...`；发布脚本执行 `python -m unittest scripts.release.tests.test_build_steps`，有其他脚本改动时按 CI 范围补测。生产 Web API 前端和 Electron 分别构建验证；新增本地连接页纳入独立构建及包内容检查，不改变原 Web 体积统计口径来掩盖增长。

真实 Electron、Windows 安装/卸载、两机组播、媒体播放与旧版升级属于必需人工/集成验收，不能以单测通过代替。无需为本计划运行构建或测试，不运行 display-scaling 专项套件。

实施新增端点/配置/架构后同步 `.cursor/rules/project-facts.mdc`、`architecture-boundaries.mdc`、必要的 `workspace-quick-reference.mdc`、README 短入口及对应语言版本、`docs/guide.md`、`CLAUDE.md` API 列表和 `docs/reference/architecture-and-implementation.html`；新增 library-config 键时同步 `docs/reference/2026-03-21-library-organize.md`。本计划仅记录目标，不提前把这些事实文档改成“已实现”。

### 10.11 审阅时需要关注的建议

无需重新讨论已明确的三种包和服务端托管 Web UI。影响工作量的剩余选择主要是：

- 首期是否按现有 Windows x64 发布链交付，NAS/其他平台安装包另列后续计划。
- Windows Server 首期是否接受现有“当前用户托盘 + 登录启动”；若要求未登录常驻，则系统服务及账户迁移必须进入本期。
- 网络范围是否先验收同机/LAN；异地 HTTPS 地址可手动输入，但公网部署能力不在本轮自动承诺。

当前计划按上述较小范围的建议编排；在开始实施前根据用户反馈调整，不据此假定额外产品需求已获确认。


## 11. 2026-09-26：拆分发行命名与版本规范

状态：用户已确认本节安装包命名与独立版本方案，Desktop 从 `0.1.0` 起步，完整稳定版为 `1.0.0`；展示不使用 beta 后缀，开发环境单独标注。已建立组件版本源与包名生成器，落地进度见 11.5。当前打包脚本仍是 Windows 一体包实现，不能把规范落盘等同于三包生成和独立更新已经完成。本节取代前文「包名尚未确定」及「两组件初期同批同版本」的建议。

### 11.1 安装包名

文件名区分大小写，产品使用 `Curated`，组件使用 `Full` / `Server` / `Desktop`；版本不带 `v`。平台统一为 `windows` / `macos` / `linux`，架构统一为 `x64` / `arm64`（若实际提供 macOS 双架构包才使用 `universal`），不得将 arm64 产物标成 x64。

| 用途 | 文件名规范 | 示例（版本仅示意） |
|---|---|---|
| Windows 完整安装包 | `Curated-Full-Setup-<bundleVersion>-windows-<arch>.exe` | `Curated-Full-Setup-1.6.0-windows-x64.exe` |
| Windows Server 安装包 | `Curated-Server-Setup-<serverVersion>-windows-<arch>.exe` | `Curated-Server-Setup-1.6.0-windows-x64.exe` |
| Windows Desktop 安装包 | `Curated-Desktop-Setup-<desktopVersion>-windows-<arch>.exe` | `Curated-Desktop-Setup-1.0.0-windows-x64.exe` |
| macOS Desktop 磁盘映像 | `Curated-Desktop-<desktopVersion>-macos-<arch>.dmg` | `Curated-Desktop-1.0.0-macos-arm64.dmg` |
| Windows 便携包 | `Curated-<component>-<version>-windows-<arch>.zip` | `Curated-Desktop-1.0.0-windows-x64.zip` |
| Linux 归档包（预留） | `Curated-<component>-<version>-linux-<arch>.tar.gz` | `Curated-Server-1.6.0-linux-x64.tar.gz` |

macOS Desktop 首选 DMG；内部应用显示为 `Curated Desktop.app`。若后续提供需要安装服务的 macOS Server / Full 安装器，预留 `Curated-Server-Setup-<version>-macos-<arch>.pkg` 与 `Curated-Full-Setup-<version>-macos-<arch>.pkg`，不能仅改扩展名生成。DMG、PKG、Linux 归档及新架构仍需各自安装/签名/升级验收，不是本次新增可发布平台。

应用身份、安装目录、签名标识、文件名分别管理。内部 npm 包标识 `curated-desktop` 不作为用户下载文件名；Web 随 Server 发布，没有第三套 Web 安装包。

### 11.2 版本归属

- **Server：** 延续现有 `1.5.x` 历史，不因拆分清零。当前 `1.5.7`，首个拆分版可规划为 `1.6.0`，实际发布前按兼容性评审确认；若破坏既有公开协议，应按 SemVer 升主版本。
- **Desktop：** 单独维护 SemVer，起点方案见 11.3。只在桌面客户端变化时升级，包括本地连接页、窗口/托盘、SSDP 客户端、桥接、安全修复及 Electron 运行时；Server 托管业务页面变化不自动增加 Desktop 版本。
- **Full：** 使用独立 `bundleVersion`（首批规划 `1.6.0`），表示这次安装组合。manifest 明确记录 `serverVersion` 和 `desktopVersion`，不能以 Full 版本覆盖二者。组件或安装器组合发生变化就发布新的 Full 版本；例如 Server `1.6.0`、Desktop `1.0.0` 组成 Full `1.6.0`，之后仅 Desktop 修复到 `1.0.1`，可组成 Full `1.6.1`，Server 仍是 `1.6.0`。关于页展示实际组件版本，Full 版本只用于安装记录。
- 同一组件的 Windows/macOS 和 x64/arm64 产物共享一次发行版本；打包格式转换、测试编译、CI 重试不递增版本。已公开版本的资产不得以不同内容覆盖，修复后发布新 patch。

### 11.3 Desktop 版本规划（已确认，开发目标已写入组件源）

采用独立产品线：起始版本 `0.1.0`，早期修复依次为 `0.1.1`、`0.1.2`，新增功能升为 `0.2.0`；完成 Server 解耦、连接切换、桥接隔离、平台安装与独立更新验收后发布 `1.0.0`。不再保留沿用旧一体包 `1.5.x` 的备选方案，也不将当前尚内置/托管后端的开发壳标为已完成的 `1.0.0`。

关于页显示 `Curated Desktop 0.1.0`；开发环境另显示「开发版」标识，不把 beta、dev 或构建时间拼进用户看到的产品版本。当前发布规范无需 beta / rc 阶段，安装包名、tag 和 manifest 的版本字段均使用实际分配的三段数字版本，不能仅在显示时截掉已有预发布后缀并将其冒充稳定发行。内部构建时间、提交 SHA 与通道保留为独立元数据。

| 变化 | Desktop 版本动作 |
|---|---|
| 窗口按钮、字体/间距、崩溃或安全缺陷修复，兼容的运行时升级 | patch，例如 `1.0.0 → 1.0.1` |
| 新增兼容的连接功能、发现方式或桌面能力 | minor，例如 `1.0.1 → 1.1.0` |
| 不兼容的公开桥接/连接配置变化或不兼容平台支持调整 | major，例如 `1.1.0 → 2.0.0` |
| 未发布的开发改动 | 保持当前开发目标数字版本，开发版标识及构建时间/提交 SHA 单独记录 |

开发版不能拿 `0.0.0` 或 `0.0.1-master` 冒充正式安装版本；开发构建不因使用数字版本就进入正式更新通道，只有明确发布并通过验收的产物才可被正式更新源索引。Electron 自身的 `42.x` 仅是运行时版本，不是 Curated Desktop 产品版本。构建信息不用作更新排序依据。

**旧壳迁移：** 已发布的一体安装包内 Electron 也使用 `1.5.x`。独立 Desktop 从 `0.1.0` 开始并在稳定后进入 `1.0.0`，不能复用旧安装/自动更新身份后直接比较这两个版本；必须使用独立 Desktop 安装身份并通过明确迁移过程识别旧一体包。旧一体更新通道仍按原版本递增。不能让安装器将新独立 Desktop 判断为旧壳降级。

### 11.4 版本来源、更新与验收边界

- 发布实现拟增加独立组件版本源，`backend/internal/version/server.json`、`scripts/release/versions/desktop.json`、`scripts/release/versions/full.json`；现有 `version.json` 在旧更新通道退役前保留为 legacy 来源。构建读取对应源并生成 Electron package、Server 链接版本及 manifest，不能手工维护多份互相矛盾的版本。
- manifest 至少含 `component/variant/version/platform/arch/format/url/sha256`；Full 再包含两组件准确版本。更新器按组件、平台、架构及通道匹配，不依靠文件名猜组件、不回退到任意 EXE。
- Release tag 规划为 `desktop-v<version>`、`server-v<version>`、`full-v<version>`。tag 前缀本身不能隔离 GitHub `/releases/latest`：旧 feed 继续只有兼容完整包，新组件必须使用隔离 feed 或组件 manifest；在迁移前不能把新三包随意放入旧客户端读取的 latest。
- 关于页分别显示本机 `Curated Desktop`（可信主进程 `app.getVersion()`）和当前连接的 `Curated Server`（health）。浏览器不显示 Desktop 行。Desktop 在可信本地流程检查自身版本，Server 独立检查自身版本；开发态、离线、无匹配系统/架构包均要准确说明。
- 本次不改当前包的实际版本、不发布安装包、不切换旧更新源。脚本落地应与阶段 5 的组件构建、内容断言、安装身份及更新迁移一起完成；禁止仅改旧打包字符串，产出名称为 Desktop 却仍含 Go/FFmpeg 的错误安装包。


### 11.5 2026-09-26 落地：版本源、命名生成器与 Desktop 关于页

已实现：

- `scripts/release/versions/{desktop,full}.json` 和 `backend/internal/version/server.json` 分别维护数字版本，当前开发目标为 `0.1.0` / `1.5.7` / `1.6.0`；这不代表这些组件已有公开发行。Server 未提前升至 1.6.0。
- `release_cli.py plan-component` 只读生成组件包名、tag、平台、架构与格式；Full 同时记录两组件的准确版本。输出带 `status: planned`，不会构建安装包、递增版本或发布。拒绝 beta 后缀及错误平台/格式组合。
- `pnpm build:electron:main` 从 Desktop 版本源和 `electron/release-config.json` 生成 `electron-dist/desktop-release.json`，随现有一体包复制；主进程通过受当前窗口主 frame / origin 校验的 `getDesktopInfo` IPC 提供 `0.1.0`。关于页独立显示 Desktop 版本与「开发版」标签，浏览器隐藏此块。客户端请求 header 同步使用组件版本。
- **对 11.4 的过渡期实现修订**：现有一体安装包 `app.getVersion()` 仍属于旧安装身份，不能为了 Desktop 组件显示改成 0.1.0。当前组件版本来自本地构建元数据；根 npm 包版本也不再用作 Desktop 展示版本。独立 Desktop 安装器将来必须从同一组件源写入 Electron package，让 `app.getVersion()` 与组件版本一致。入口缓存参数继续使用原安装身份版本。
- `checkDesktopUpdate` 由主进程读取本地固定配置，renderer 不可传入更新源。开发版返回 `development`，现有一体包返回 `bundled`，独立包未配源返回 `not-configured`；均不访问正式源。预留的独立包检查使用 HTTPS manifest（禁止重定向、15 秒超时、最大 1 MiB），严格匹配 Desktop / standalone / stable / 当前平台和架构 / EXE 或 DMG，验证数字版本、HTTPS 资产 URL 与 SHA-256 字段后比较版本。不回退任意 EXE，不将无匹配包当成已是最新版。
- 发现独立包更新后仅提供下载安装包链接；尚不具备 Desktop 下载校验、自动安装和重启。SHA-256 字段校验只是 manifest 格式校验，不等于已经验证下载文件。

当前 `electron/release-config.json` 仍为 `distribution: legacy, updateFeed: null`。旧 `pnpm release:publish` 的包名、版本来源和更新源保持兼容；旧 assembler 拒绝混入独立 Desktop 身份。独立三包生成、安装身份迁移、Server 独立更新、macOS DMG/签名和正式 feed 发布仍待阶段 5，不应通过单独把配置改为 desktop 来宣布拆分完成。

验证：发行 Python 测试 19 项、Electron 测试 43 项、关于页组件测试 7 项通过；Electron 编译、前端 Web API 生产构建和相关 ESLint 通过，构建体积无提醒。已重启 macOS Electron 开发桌面，在关于页实际确认 0.1.0、开发版标识和检查按钮返回的开发态说明；当前深色窗口无文字/按钮重叠。未生成或发布正式安装包；跨平台、DPR、90%–150% 缩放与浅色完整矩阵未执行。

2026-09-26 交互调整：关于页仅保留一个「检查更新」按钮，并行检查安装包和 Desktop，等待两项结束后恢复可用；Desktop 区块保留版本及检查结果，移除独立检查按钮。安装包不支持检查时仍可检查 Desktop；Web 只检查安装包。组件测试 8 项、类型检查和相关 ESLint 通过，macOS 开发桌面实测统一按钮及结果布局正常；未扩展显示缩放矩阵。


### 11.6 Server 三段版本与独立构建信息

按用户确认，Server 与 Desktop 均维护三段数字版本；Server 当前 1.5.7，Desktop 当前 0.1.0。Server health 的 `version` 从时间戳改为产品 SemVer，新增 `buildStamp` 保留 UTC 构建戳，`channel` 与旧 `installerVersion` 独立保留，HTTP/stdio 一致。关于页用主行显示产品版本、独立开发版标签，次行显示构建时间戳。侧栏显示产品版本，启动日志与导出身份保留产品版本和时间戳。

Server 唯一来源迁至 `backend/internal/version/server.json` 并由 Go embed，组件包名生成器同步读取；这让直接 go run / go build 也有正确版本，不依赖预生成或手工维护第二份版本常量。正式构建继续注入 BuildStamp；普通 Go 构建缺少注入时仍回退 vcs.time / revision / unknown，不能把 Git 提交时间视为编译时刻。旧版本字段类型不变；依赖其时间戳语义的调用方需改读 buildStamp。

验证：Go version/server/app 测试及 release 标签的 version 测试通过；前端相关 18 项测试、类型检查、ESLint 通过；发行脚本 19 项测试通过。关于页许可测试同步补齐此前已添加的 Outfit 许可数量。完整跨平台显示缩放矩阵未执行。

已重新编译并重启开发桌面：实际 `/api/health` 返回 `version: 1.5.7`、`buildStamp: 20260926.123433`（UTC）、`channel: dev`，侧栏显示 `1.5.7 (dev)`。Web API 生产构建通过且体积无提醒。

2026-09-26 展示补充：Desktop 构建脚本将编译时 UTC 时间戳写入 `desktop-release.json.buildStamp`，由 `getDesktopInfo()` 提供，关于页与 Server 一样在版本下独立显示；重启不改变构建时间，不参与版本比较。两区块并列，Server 标题简化为「Curated Server」，不再附加「版本号」。

## 12. 2026-09-26 切换入口位置讨论（建议，待用户确认）

本轮仅讨论交互，不实施功能。暂将用户所说的“切换 desktop”理解为“在 Desktop 内切换连接的 Server”；若指浏览器打开桌面应用，则属于另一项交互需求，需要另行明确。

当前核对：组件版本与关于页展示已分开，但 `electron/main.ts` 仍先执行 `startBackend()`，之后创建业务窗口；尚无用户可操作的连接管理。`AppSidebar.vue` 的底部已有服务器状态、版本和重试按钮，紧邻设置入口。

建议入口布局：

- 主入口放在侧栏底部，升级现有服务器状态区。显示状态点、当前服务器名称与展开箭头，例如“家庭 NAS”；地址作为次行，版本放到展开详情或关于页。侧栏收起时保留可点击的服务器图标和名称提示，不能只剩不可操作的状态点。
- 点击打开小型菜单，显示当前连接、最近使用的服务器、“连接其他服务器…”和“管理连接…”。选择新目标后再检查连接和认证，不将切换伪装成即时成功。
- 设置 → 网络与设备内增加清晰标为“此桌面端”的连接管理区域，管理已保存地址、名称和最近连接；与当前服务器的局域网开放、插件联动等配置区分。
- 桌面应用菜单／托盘提供“更换服务器…”，进入随 Desktop 提供的本地连接页。首次未连接、服务端页面加载失败或 PIN 锁定时也应能进入；不能只依赖服务端页面内的侧栏和设置。

位置取舍：侧栏底部延续现有状态区，适合偶尔在本机与 NAS 之间切换；若日常高频在多台服务器间工作，则可考虑在侧栏顶部品牌下增加当前服务器选择器。只放设置内发现成本较高；关于页主要承担版本与更新信息，不建议作为切换主入口。

沿用第 3 节边界：普通空闲切换直接执行；正在播放、上传或有未保存编辑时说明影响并处理；服务端已提交的扫描等任务继续。入口文案建议使用“切换服务器”或“更换服务器”，具体位置与措辞仍待讨论确认。

## 13. 2026-09-26 远程连接时的更新归属（用户已明确，待实施）

用户明确：更新操作先判断 Desktop 与当前 Server 是否在同一台机器；远程连接时只更新当前机器的 Desktop，不关闭、重启或更新远端 Server。未来可以提供从远端发起 Server 更新的方式，本期不添加。此次延续交互讨论，仅记录产品规则，不代表更新功能已实施。

### 当前代码事实

- `SettingsAppUpdateSection.vue` 中“下载并安装”调用 `useAppUpdate().downloadInstaller()`，经 `POST /api/app-update/download` 让 Server 下载并校验；之后“立即安装”调用 `/api/app-update/install`，由 Go `appupdate.Service.Install` 在服务端机器启动安装器。不能认为当前按钮已经按客户端所在机器更新 Desktop。
- 独立 Desktop 检查经 Electron `checkDesktopUpdate()`，按 Desktop 组件、系统和架构匹配产物。目前只提供下载链接，尚无 Desktop 下载校验、自动安装和重启流程；当前 legacy 发行配置也没有独立更新源。

### 已确定的产品边界与建议交互

- 远程 Desktop：只使用 Desktop 自身版本、更新源和匹配当前机器系统／架构的独立 Desktop 包。下载、校验和安装由当前机器的桌面端负责；不得调用远端 Server 的下载、安装或清理更新包接口，也不得回退到 Full／legacy 一体包。
- 建议把远程场景的按钮明确写为“下载并安装 Desktop”，旁边说明“仅更新此电脑上的 Desktop，服务器继续运行”。Server 版本仍可展示，服务端更新提示应指向在服务端机器操作，不提供远程安装动作。
- 尚无可用独立 Desktop 包或自动安装能力时，应如实显示不可用状态或“下载 Desktop 安装包”的已有能力，不能把下载链接标为已具备自动安装能力，也不能因 Server 有更新就显示 Desktop 可更新。
- 同机场景保留本机更新需求，但具体如何覆盖独立组件与旧一体包，需要结合发行类型定义；不能仅因“同机”就自动更新两个组件。
- 普通浏览器没有 Desktop 桥接，不展示 Desktop 自动安装能力。客户端触发的后台自动下载也需遵循相同归属，不能在远程场景调用旧 Server 下载接口。

### 判断原则（实现建议，待落实）

由桌面主进程根据实际连接目标及可信的本机服务关联提供判断，不只依赖页面是否运行在 Electron、前端页面地址或客户端／服务端版本相同。开发 Vite 代理、本机 LAN 地址和反向代理可能使地址表象与实际归属不一致；无法确认同机时按远程限制处理，不授权服务端更新。未来独立 Desktop 更新器还须确保退出 Desktop 不走旧壳停止托管 Server 的生命周期逻辑。

验收应覆盖：远程 Desktop 更新不调用 Server 更新接口且 Server 任务继续；Desktop 已最新但 Server 有更新时不误报 Desktop 更新；无匹配 Desktop 包时不下载 Full；无法确认连接归属时不进入本机 Server 更新；远程浏览器不显示 Desktop 安装动作。以上均为待实施验收项。


### 13.1 实施：更新归属隔离（2026-09-26）

本次落实远程更新边界，不扩展独立安装器／发布源：

- Desktop metadata 新增主进程实际 `serverOrigin`。前端结合实际 API、页面 loopback 地址及 legacy distribution 判断本地旧安装器适用性；standalone Desktop 始终用自身通道。缺字段、桥接失败与不能确认同机均不放行。
- 既有 HTTP 更新 DTO 新增请求级 `localUpdateAllowed`（不写入更新缓存），响应 no-store；后端下载、安装、清理接口校验 socket peer、Host、可选 Origin 与代理头。非本地返回 403 `APP_UPDATE_REMOTE_DISABLED`，不调用更新 provider。反向代理须保留外部 Host 或转发头；剥离全部来源信息的代理无法由应用可靠识别，应避免代理这些写接口。
- 前台按钮、已校验安装包、清理与启动自动下载使用相同限制；远程／未知连接隐藏 Server 自动下载配置开关；写操作前重新读取权限。远程 Server 的更新不会生成 Desktop 更新徽标／通知，也不展示一体包 Release 链接或说明。
- 远程 Desktop 的统一检查按钮仅检查 Desktop，Server 版本仍显示；匹配独立包时显示“下载 Desktop 安装包”及手动安装说明，legacy 显示尚不支持独立更新。不宣称自动安装、不回退 Full，不退出或重启 Server。
- 无 HTTP 新端点、数据库迁移或配置字段。需重新运行新版 Server、重启 Desktop 使桥接新字段生效；旧 Server／旧桥接按未确认同机处理。连接切换入口（§12）仍属讨论建议，本次未实现。


验证记录：前端更新与关于页 38 项测试、自动下载区块 2 项及 SettingsPage 4 项通过；Electron 全部 43 项通过；Go server/appupdate/contracts 全包测试与 vet 通过；相关 ESLint、vue-tsc、Electron 编译及 Web API 生产构建通过（体积 0 提醒／超限）。Playwright 在现有开发页注入远程 Desktop 桥接及 Server 已校验安装包响应，确认点击检查只显示 Desktop 下载，Server 安装动作、Release 与自动下载开关隐藏；请求记录无 Server 更新写请求。1200×约 740 的深色浏览器截图已检查，无重叠；本地证据在 `.workspace/update-target/`。未实际下载／运行安装器、未重启现有 Server／Desktop、未运行跨平台缩放矩阵，未生成或发布安装包。实现提交：`30b3dbbf`。


## 14. 远程存储目录只读（2026-09-26，已实施）

用户确认：存储路径属于 Server 文件系统，远程 Desktop（例如 Mac 连接 Windows Server）只展示路径信息，新增、删除、修改等管理操作留在服务器所在电脑。

- 影片、漫画、写真库的远程路径区域使用同一个只读组件：显示库名称、完整服务端路径、默认导入目录标记；影片库继续显示已有存储状态。不呈现新增／删除／重命名、默认目录选择、硬盘重新绑定、本机目录选择和打开目录入口。路径区域原有扫描／批量元数据刷新等操作栏也不在只读视图中显示。
- `GET /api/health` 增加请求级 `canManageLibraryPaths` 并使用 no-store。前端结合实际 API、页面及 Desktop 主进程提供的 `serverOrigin` 判断；不依赖客户端操作系统。独立 Desktop 同机也可管理，不套用 legacy 更新包限制。缺字段、桥接错误或请求失败默认只读；Mock 仍允许编辑演示数据。
- Server 复用更新入口的直接本机判断为 `isDirectLocalRequest`，三库路径 POST/PATCH/DELETE 与影片目录 reveal/rebind 共 11 条路径写路由加本机限制，远端返回 403 `LIBRARY_PATHS_READ_ONLY`。`PATCH /api/settings` 的三个默认导入目录字段同样限制；混合请求在任何设置写入前整体拒绝，不会部分应用。
- 本机管理需通过 localhost／loopback 直连，socket peer、Host、可选 Origin 均满足本机要求且无代理转发头；LAN 地址与无法确认的代理连接保守只读。代理必须保留外部 Host 或转发头，剥离全部来源信息的代理无法识别为远程，不应转发这些管理接口。
- 远程上传影片仍使用服务端已配置默认目录。路径读取、存储检查和其它业务 API 保持原行为；本次没有把所有扫描、媒体元数据或其它设置操作改为本机专属，也没有迁移路径数据或修改现有 library-config.cfg 字段。
- 需运行新版 Server 并重启新版 Desktop 使能力信息生效；旧 Server／旧桥接显示只读。

验证：62 项相关前端测试、类型检查与相关 ESLint 通过；Go server/contracts 包测试、远程接口与实际上传回归、相关 vet 通过；Web API 生产构建通过。Playwright 通过模拟 Mac Desktop 桥接、远端能力与 Windows 路径，检查影片／漫画／写真设置的只读显示及默认目录标记，截图无重叠；证据保存在 `.workspace/readonly-paths/`。未做真实双机联调、未重启现有 Server／Desktop、未运行跨平台显示缩放矩阵，未生成或发布安装包。


## 15. 2026-09-26 Desktop 服务器列表与切换（已实施）

本次交付保存名称与 HTTP/HTTPS 根地址、编辑/删除、切换与记住上次连接。列表保存在 Desktop userData，业务页面继续加载 Server Web UI；旧一体包无已保存目标时保留本机启动。已保存目标或显式远端连接失败不启动本机后端。SSDP、独立安装器和 Server 生命周期拆分不在此切片。

设计：属于桌面壳连接配置面，列表“连接”为主行动，表单和编辑/删除为辅；沿用设置页纵向卡片与语义颜色。侧栏增加能力检测入口，托盘始终可打开本地窗口。独立本地 HTML 窗口为明确局部例外：不引入整套业务 Vue 依赖，构建时从 src/style.css 提取主题令牌，使用系统字体和原生语义控件。涵盖首次空态、连接中、离线错误、保存错误、删除确认、切换影响确认与窄窗口换行。远程页面只获打开窗口能力，列表管理 IPC 仅本地窗口可调用。按服务器 origin（含端口）隔离持久会话；切换成功后销毁旧 renderer，失败保留旧连接。


本次实现：8 秒 Curated health 校验、15 秒页面加载上限；成功后记录目标并销毁旧窗口，失败保留当前窗口。列表最多 100 项，按标准化 origin 去重；不在线也可保存。当前连接需先切换才可删除/改地址，可直接改名。应用菜单/托盘支持旧 Server 页面，侧栏入口按桥接能力隐藏于 Web。仅 legacy 默认本机目标保留原进程启动，避免已保存本机用户下次启动失效；其它目标失败保持连接窗口，不回退。本次无新增 HTTP 端点和 library-config 字段。配置损坏会报错并保留原文件，不静默重置。Cookie/存储独立分区；旧共享会话不迁移，首次升级可能需重新解锁。


验证：Electron 58 项单测、侧栏 13 项单测、前端类型检查与相关 ESLint、Electron 编译通过；Web API 生产构建通过，体积 0 提醒/超限。使用独立临时 userData 的真实 macOS Electron，连接两个本地 HTTP 测试服务器，实测添加/保存、A→B、同主机不同端口 Cookie/localStorage 隔离、加载期 Desktop metadata、远端无列表管理桥接、离线目标失败保留 B、重启恢复 B。760×692 CSS px / DPR 2 / 默认 100% 缩放的浅色列表与失败态截图已检查，无重叠，证据在 `.workspace/server-connections/`。未触碰现有桌面配置，未重启用户现有 Server/Desktop；未验证真实跨机器网络、Windows 安装包、PIN 端到端与完整跨平台/深色/显示缩放矩阵，未执行 `pnpm test:display`、未生成或发布安装包。


### 15.1 设置内的连接概览与列表（2026-09-26）

用户要求在设置显示连接情况、当前服务器及已保存列表。设计归属为「设置 → 网络与设备」首张卡片，沿用 SettingsNetworkSection 的 Card/Header/Input/语义状态色：当前名称/地址和在线状态优先，保存列表次之，添加/管理为局部操作。Desktop 新增只读列表桥接，保存仍在本地管理窗口，列表选择只能传已保存 ID 并经原生切换确认。Web/旧桥接隐藏此桌面专属区；读取中、空列表、读取失败/重试、连接中、离线均有状态。窗口重新获得焦点及分区挂载期间轮询刷新保存列表，退出清理监听与计时器。没有全局令牌变更。


验证：设置相关 22 项单测（含新增区块 7 项）、Electron 58 项、类型检查与相关 ESLint 通过；Web API 生产构建通过，体积无提醒。独立临时 userData 的真实 Electron 验证只读列表 IPC、无效 ID 拒绝、由 Server renderer 发起已保存 ID 切换及既有隔离/失败恢复回归。Playwright 在现有开发页注入 Desktop 桥接，1280×720 / DPR 1 / 默认缩放的深色设置卡片截图检查正常；点击添加、连接分别传入管理入口与准确保存 ID，截图 `.workspace/server-connections/settings.png`。未重启用户现有 Desktop/Server；未运行完整跨平台显示缩放矩阵、未打包发布。


2026-09-26 入口收敛：按用户要求移除侧边栏「服务器」按钮；日常连接管理统一从设置 → 网络与设备 → 服务器列表进入，托盘/macOS 应用菜单继续保留离线可用的本地管理入口。连接数据与切换行为不变。


### 15.2 已保存服务器列表样式（2026-09-26）

按用户反馈，将设置中的已保存服务器改为统一边框与分隔线的紧凑列表。沿用设置 nested 区块 p-4、gap-3、语义颜色；每行服务器图标、名称/地址、右侧操作对齐，当前连接标记占同一操作列。列表容器宽度达到 32rem 时名称和地址分列，否则地址放到名称下一行；长文本省略并提供完整 title。连接按钮桌面保持 32px、窄屏至少 44px。空态/错误/忙碌行为、服务层、数据与连接确认不变；无全局令牌或共享组件调整。

验证：该区块 7 项行为测试、前端类型检查、相关 ESLint 通过。已在正在运行的 macOS Desktop 深色设置页检查实际效果，当前服务器名称、地址与标记按列表列对齐，热更新已生效；未运行完整跨平台显示缩放矩阵。


### 15.3 页面内添加服务器对话框（2026-09-26）

用户要求添加服务器无需独立原生窗口。设置的“添加服务器”采用现有 Dialog/Input/Button 基元，沿用 SettingsLibraryPathAddDialog 的名称/地址表单与右侧取消/保存；短标题、可滚动限高、语义输入边界，保存中禁止重复提交与关闭，失败保留草稿。保存成功关闭并更新列表，不自动切换。当前主窗口主 frame 获得受校验的 addServer 窄能力，仅新增，禁止传 ID 覆写/重复地址，主进程继续执行地址校验和本地原子保存。旧主进程缺少 handler 时明确提示完全退出重启。管理/离线恢复窗口继续承担既有编辑删除。


验证：添加弹框与列表共 13 项前端测试、59 项 Electron 测试、类型检查、相关 ESLint、Electron 编译与 Web API 生产构建通过（体积 0 提醒/超限）。独立临时 userData 的真实 Electron 验证新增记录持久化、重复拒绝、当前连接不变、不新增原生窗口，并回归切换/会话隔离/失败恢复。Playwright 注入测试桥接验证页面内保存后列表更新，检查 1280×720 与 375×812、DPR 1、默认缩放的深色弹框，无重叠或越界；证据位于 `.workspace/server-connections/add-dialog*.png`。未重启用户正在使用的 Desktop，启用新保存 IPC 需完全退出重开。未运行 `pnpm test:display` 或完整跨平台/浅色/显示缩放矩阵，未生成安装包。

### 15.4 设置归属标签（2026-09-26）

设置卡片标题旁用现有 outline Badge 标记配置归属，保持低强调度、可换行，不增加说明段或改变保存行为。Server 表示当前服务器维护的配置/数据；Desktop 表示此桌面端；客户端 localStorage/IndexedDB 偏好在 Desktop 中标为 Desktop，在浏览器中标为 Browser。标签用悬停说明区分客户端偏好与桌面壳配置，按实际存储归属判断，不能按功能名称猜测。

归属：服务器列表是 Desktop；语言/外观、前端日志、萃取快捷键/保存策略/反馈音/本机导出目录及原生播放器协议模板是客户端本地。开机启动、自动下载更新、三库及阅读器默认配置、元数据、代理/LAN、插件联动、连接设备、PIN、AI、维护、后端日志均由 Server 管理。播放与萃取帧卡片显示双标签，并在内部具体配置标题标注；关于页的更新区按实际目标标注，许可证等通用信息不强行归类。覆盖加载/只读/禁用状态，复用固定归属标签，不把在线状态或权限与归属混为一谈。无全局颜色或密度令牌变更。

验证：设置组件原有 187 项测试与新增归属标签 3 项测试通过（修复受新增 Badge 影响的局部 UI mock；Node 运行时使用 `NODE_OPTIONS=--no-experimental-webstorage` 避免实验性 localStorage 覆盖 DOM 测试环境）。类型检查、相关 ESLint、Web API 生产构建通过，体积 0 提醒/超限。Playwright 注入 Desktop 测试桥接，检查通用、网络、萃取帧的归属标签及深浅色、1280×720 和 375×812 默认缩放布局，未发现标题/标签重叠；未修改真实配置，未重启 Desktop。证据位于 `.workspace/server-connections/settings-scope-*.png`；未运行 `pnpm test:display` 或完整跨平台/DPR/缩放矩阵。
