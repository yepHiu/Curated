---
description: Curated UI layering, design tokens, surfaces checklist; index to vue/jav-library rules
globs: src/**/*.{vue,css}
alwaysApply: false
---

# UI Component Spec（Curated）

萃取帧反馈采用局部 `CaptureReceipt`，成功后显示准确媒体时间与缩略图，失败提供受限重试，不连续堆 Toast；`FrameImageViewer` 提供当前图加载/重试及 100% 查看。播放器中的 Dialog / DropdownMenu 通过 `portalTo` 放入全屏 surface，避免全屏时被遮挡；不改变其它页面默认 portal。

人读详解与维护约定：**[`docs/reference/2026-03-24-frontend-ui-spec.md`](docs/reference/2026-03-24-frontend-ui-spec.md)**（仓库根相对路径）。表单控件细节：**[`vue-frontend-standards.mdc`](vue-frontend-standards.mdc)**。业务页面体验：**[`jav-library-frontend-patterns.mdc`](jav-library-frontend-patterns.mdc)**。

## 分层（MUST）

- **`src/components/ui`**：`shadcn-vue` 基元与通用复合件（Button、Card、Dialog、Input…）。不放影片/库领域逻辑。
- **`src/components/jav-library`**：Curated 领域 UI（`MovieCard`、`DetailPanel`、`PlayerPage`…）。组合 `ui`，经 **`useLibraryService()`** / **`src/api`** 取数，不绕过服务层。
- **`src/views`** + **`src/layouts/AppShell.vue`**：路由级页面与壳层；以组合 `jav-library` / `ui` 为主，避免在 view 里堆叠一次性大块业务逻辑。

## 视觉令牌（MUST / SHOULD）

- **MUST** 使用语义 Tailwind / 主题类：`bg-background`、`text-foreground`、`border-border`、`bg-card`、`text-card-foreground`、`bg-muted`、`text-muted-foreground`、`primary`、`destructive`、`ring` 等。CSS 变量事实源：**`src/style.css`**（`:root` 已为深色主界面）。
- **MUST NOT** 在业务组件中随意写死 `#rrggbb` 或任意 `rgb()` 作为主色/背景（难以随主题维护）。
- **字体**：全局正文由 `--app-font-stack` 控制；中文界面汉字使用 HarmonyOS Sans SC，日语界面假名与汉字使用 Noto Sans JP，拉丁字母与数字使用 Noto Sans。`html lang` 选择中日字形顺序，资源均本地打包。侧栏 **Curated** 字标继续使用 **`text-primary`** + **`font-curated`**，字型改为 Noto Sans。

## 深色与 `dark:`（MUST 理解）

- 默认主题在 **`:root`**；`html` 往往 **无** `class="dark"`。项目定义 `@custom-variant dark (&:is(.dark *))`，故 **`dark:*` 仅作用于 `.dark` 祖先内部**。
- 主界面表单与卡片对比度：**不要**单靠 `dark:*`；用 **`border-border/…`、`bg-muted/…`、`text-foreground`** 等始终生效的令牌。详见 **`vue-frontend-standards.mdc`**「Form controls」。

## 表单与对话框（MUST）

- 使用 **`<Input>`** 时 **`import { Input } from '@/components/ui/input'`**（显式导入）。
- 同一表单内 **`<Input>` 与 `<textarea>`** 边框与弱填充语言一致（Dialog / Card 表面可读）。不得把主输入做成「透明底 + 极淡边」导致看不清。

## 产品面速查（SHOULD）

- **浏览**（`library` / `favorites` / `recent` / `tags`）：海报优先、卡片紧凑；整张可点进详情；网格与「相关推荐」尺寸变更时保持一致，除非任务明确要求分叉。筛选弹层、工具栏、区块标题下**不要**加教学型说明段；见 **`product-rules.mdc`**。
- **详情**：左海报右元数据，层次清晰；避免巨型 hero 营销布局。
- **播放**：video-first、低干扰控件；勿加与视频竞争注意力的说明卡片（除非明确要求）。
- **设置**：居中内容带、纵向设置卡片；避免无故改成宽表格式后台。Tab 大卡片的信息架构、标题行、间距与 nested 块规范见下节 **「设置页 Tab 大卡片与内嵌区块」**。
- **壳层**：保留 **`AppShell`** 内滚动：内容区滚动，避免整页壳跟着滚。

## 设置页 Tab 大卡片与内嵌区块（MUST / SHOULD）

面向 **`SettingsPage`** 等以 **Tab / 分区大卡片**（`Card`）组织的界面：须区分 **卡片级标题**、**内嵌功能块标题** 与 **说明文案**，并保持对齐、字号与垂直节奏可预期。

### 信息架构（SHOULD）

- **`CardTitle`（卡片级）**：只承载本 Tab 或分区的**短标题**（例如元数据：`SettingsMetadataSection` 中「影片元数据刮削源」）。不在这里堆长说明。
- **`CardDescription`（卡片级）**：**默认不要**。浏览面和控制面靠短标题即可；设置页仅在操作后果不明显时才写一句。
- **说明克制**：不要给每个 nested 块、每个选项都加 hint。不可逆/危险操作可在确认框写一行后果；其余行为写进规则和 `docs/`，不堆在界面上。

### 卡片标题行布局（SHOULD）

- **`CardHeader`** 内采用 **`grid grid-cols-[auto_minmax(0,1fr)] items-center gap-x-2.5`**：左侧 **`size-9`** 图标区（`rounded-lg border border-primary/25 bg-primary/10 text-primary`），右侧 **`CardTitle`**：`min-w-0 text-lg tracking-tight`（字重沿用 `CardTitle` 默认 `font-semibold`）。
- 纯装饰图标使用 **`aria-hidden="true"`**。

### 大卡片垂直节奏（SHOULD）

- 单张业务 `Card` 可收紧 **标题栏与 `CardContent` 首行** 的间距：在 **`SettingsMetadataSection`** 范式下——**`Card` 追加 `gap-2`**（覆盖基类较大 `gap`）、**`CardHeader` 使用 `pb-0`**、**`CardContent` 使用 `pt-0`**。
- **`CardContent`** 内多个子组件 / 嵌套块之间：**`flex flex-col gap-3`**（与同页其它 settings 栈一致），除非整 Tab 统一改为另一种间距令牌。

### 内嵌浅色块：标题与说明（MUST）

同一 **`CardContent`** 下并列的 **nested 区块**（`rounded-* border border-border/… bg-muted/…`，如连通性、自动化开关、策略、列表、触发操作）须保持 **块标题与块说明左缘对齐**，且彼此 **`p-4`** 一致（与项目既有 nested 卡片规则一致）。

- **块标题**：`text-sm font-semibold text-foreground`。
- **块说明 / 提示**：`text-xs leading-relaxed text-muted-foreground sm:text-sm`；长段落可加 `text-pretty`。
- **块内标题与说明纵向间距**：同列默认 **`flex flex-col gap-3`**；需要更紧凑时（如首块工具行旁短文）可用 **`gap-2`**（见 **`SettingsMetadataAutomationSection`** 源连通性）。
- **禁止**：仅在某一嵌套块的标题行增加额外 **`px-*`** / **`px-0.5`**，导致与同卡其它 **`p-4`** 块水平起点错位。

### 选项组与横向操作（SHOULD）

- **互斥策略**：使用 **`fieldset`** + **`legend.sr-only`**；每项为 **`label` > `input[type=radio]` + 文案**；行布局 **`items-center`**，使圆点与多行主副文案垂直居中（**`SettingsMetadataModeSection`**）。
- **选项主文案**：`text-sm font-medium`；**选项下说明**：与上节「块说明」同款 token（`text-xs … sm:text-sm`）。
- **文案列 + 主行动**：**`flex justify-between gap-3`**；在 **`sm:flex-row`** 下对整块使用 **`items-center`**，避免按钮相对双行说明「贴顶」（**`SettingsMetadataTriggerScrapeSection`**）。
- **设置页密度例外**：`SettingsPage` 默认把常规按钮/选择器收紧到 32px；需要手机端舒适触控的主要维护动作可加 `data-settings-comfortable-control` 退出该全局约束，并在自身使用 `h-auto min-h-11`，保证实际按钮框至少 44px。不要把该 opt-out 机械扩散到所有设置控件。

### 参考实现

- **`SettingsMetadataSection.vue`**（大卡片壳层、`CardHeader` 网格、间距范式）
- **`SettingsMetadataAutomationSection.vue`**、**`SettingsMetadataModeSection.vue`**、**`SettingsMetadataProviderChainSection.vue`**、**`SettingsMetadataProviderSelectSection.vue`**、**`SettingsMetadataTriggerScrapeSection.vue`**

## 显示缩放（MUST）

- 修改 **壳层布局、网格、卡片、播放器 HUD、设置页、对话框、全局字体或 spacing** 时，必须按 **[`docs/reference/frontend-display-scaling-checklist.md`](docs/reference/frontend-display-scaling-checklist.md)** 做多端显示缩放检查。
- **MUST NOT** 未经用户明确同意主动运行 **`pnpm test:display`** 或其他 display scaling 冒烟测试；该测试耗时较长，只在用户确认、发布前检查或明确需要验证显示缩放回归时运行。
- 普通布局以 CSS pixel 为基准；不要为每一种 DPR 或物理分辨率写独立业务布局分支。
- 桌面 Retina 紧凑密度仅通过 `src/style.css` 全局变量和 `(hover: hover) and (pointer: fine) and (min-width: 1024px) and (min-resolution: 2dppx)` 媒体查询实现；DPR `1.5` 外接屏必须继续走默认密度变量。
- 显示适配不应绕过服务层或把业务能力判断塞进页面组件；浏览器前端不默认假设 Electron / mpv 存在。
- 不因显示适配一次性引入 Pinia/Vuex 等全局状态管理重构；如后续需要 Pinia，应从小服务或小模块逐步试点。

## 交互与可访问性（SHOULD）

- 可点击海报/卡片主区域用 **`button type="button"`** 或明确 `role` + 键盘可操作；保留 **`focus-visible`** 样式（勿 `outline-none` 且无替代）。
-  meaningful 图片提供 **`alt`**（如番号）；装饰性渐变层 **`aria-hidden="true"`**。
- 手机宽度下主要触控目标（壳层菜单、导入、通知、主题切换、资料库筛选/排序/批量入口与影片勾选）须至少 **44×44 CSS px**；桌面紧凑密度可在 `lg` 断点恢复既有尺寸。
- `LibraryPage` 不再展示入库/发售/评分三列 Tabs；排序只走工具栏排序按钮。每个路由页面保留唯一语义 `h1`（可在视觉上 `sr-only`）。
- `MovieCard` 标签可收缩并省略，但末尾 `+N` overflow badge 必须 `shrink-0` 且保持在卡片边界内。

## 嵌套下拉 / 二级菜单（MUST）

下拉菜单再向一侧展开另一级面板时（`DropdownMenuSub` / `DropdownMenuSubContent`，以及同类 Popover 二级面），必须当成**两个独立浮层**来排，而不是贴在触发行旁边的附属条。

- **水平间距**：二级面板与父菜单之间使用 **`sideOffset: 8`**（`DropdownMenuSubContent` 默认值）。一级菜单相对触发按钮仍用 **`sideOffset: 4`**。禁止把二级 `sideOffset` 留在 `0`，以免两个 `rounded-2xl` 面板贴边或视觉重叠。
- **顶对齐**：与父菜单并列的**伴随面板**（命名、筛选摘要、短表单）必须与父菜单**顶边对齐**，外边缘水平间距为 8px。菜单项锚点在父菜单 `p-1 border` 内时，书签命名局部使用 **`side-offset: 13`** / **`align-offset: -5`** 补偿内边距与边框，并设 **`align-flip: false`**。共享子菜单默认仍为 8；验收应测量面板实际边缘。
- **行对齐例外**：指向某一菜单项的操作子菜单（应用 / 重命名 / 删除）仍对齐该行；但同样遵守 8px 水平间距。
- **禁止**：二级面板与父菜单重叠、错开顶边却没有明确的行锚点、用全宽主按钮把短表单撑成对话框。
- **短表单主按钮**：放在面板内右下；**胶囊形**（`rounded-full`），按内容宽度收缩，**不得 `w-full`**。
- **书签命名**：使用 `p-4` 与 `FieldGroup` / `Field` / `FieldLabel` / `Input`。小于 640px 时关闭父菜单并用独立 `Dialog`，保存和关闭按钮至少 44px；桌面保留紧凑尺寸。Tab / Enter / Esc 可用，关闭后焦点返回入口；失败保留草稿，忙碌时禁止重复提交。

参考：`LibrarySavedViewsControls` 的书签菜单与「保存当前视图为书签」命名面板。

## 阅读器底栏 HUD 菜单（MUST）

漫画 / 写真阅读器底栏的设置菜单必须出现在**整条工具栏上方**，与工具栏外缘保持 **8px** 间距，不得覆盖底栏。以底栏卡片为定位锚点（`data-book-reader-chrome`），`side="top"`、`sideOffset: 8`，并关闭 `sideFlip`，避免空间不足时翻到工具栏下方。这是播放/阅读面的局部例外，不改变普通页面一级菜单默认 `sideOffset: 4`。

## 变更同步

修改 **`src/style.css`** 全局令牌或 **`components/ui`** 中共享基元默认样式时：同步更新 **`docs/reference/2026-03-24-frontend-ui-spec.md`** 与本文件相关条目。

## 个人洞察（2026-09-24）

`PersonalInsightsPage` 保持 AppShell 内部滚动与 Card/Progress 基元。范围选择在桌面按内容收缩，手机端为 2×2 且每项至少 44px；六项摘要桌面 3×2、手机 2 列。演员、厂牌、标签各默认显示前五项，可独立展开与重试；页面不显示重复的页首说明、统计口径折叠区或归因提示。范围切换保留旧结果时明确标注旧范围，不得把旧数据当作新范围显示。

## macOS 融合窗口栏（2026-09-26）

仅在 preload 提供 `windowChrome: "macos"` 时启用：顶部工具栏与侧栏品牌区为 64px，原生红黄绿按钮预留左侧 96px；收起侧栏保留 96px 并隐藏品牌链接。窄窗口工具栏和侧栏抽屉避让按钮。顶部空白区域可拖动，交互控件区域使用 `no-drag`。主题沿用 sidebar/background 令牌；Web 与其它桌面平台保持原样。阅读器与独立锁屏/故障页保留顶部拖动区域。
