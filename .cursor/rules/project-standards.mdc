---
description: Minimal always-on repo identity and rule handoff
alwaysApply: true
---

# Project Standards

- **Product name: Curated** (repo directory / npm package id may remain `jav-shadcn`). This repo is a **`Vue 3 + TypeScript + Vite` + `shadcn-vue`** app at the repo root, with a **`Go` HTTP backend** under **`backend/`** (not a frontend-only scaffold).
- Separate **current code facts** from **future desktop (Electron / IPC / mpv)** plans; see `architecture-boundaries.mdc`.
- Primary rule handoff: **`workspace-quick-reference.mdc`** (daily ops) → **`project-facts.mdc`** (structure/API) → **`git-commit-workflow.mdc`** (最小单元提交；仅用户要求时再 push) → **`ui-component-spec.mdc`** (UI layering, tokens, surfaces) → **`vue-frontend-standards.mdc`** (incl. **Form controls** / `Input`) / **`jav-library-frontend-patterns.mdc`** (library UX) → **`backend-*`** rules when touching Go.
- **Planning docs**: 后续任务规划、实施计划、路线图等 Markdown **plan** 文档统一放在 **`docs/plan/`**；若目录不存在则创建后再写入（勿把 plan 散落在 `docs/` 根目录；**`docs/README.md`** 说明 `reference/`、`product/`、`ops/`、`review/` 等用途，长文与规范各归其位）。操作细节与跨目录文章索引写在 **`docs/guide.md`**，根 README 保持短入口。
- **Plan persistence**: 当代理输出“方案 / 计划 / 路线图 / 实施建议”等可复用内容时，除对话回复外，还应同步落盘到 **`docs/plan/*.md`**；同主题优先更新已有文档，避免重复创建近似 plan。
- **Encoding rule**: 写入项目规则、项目记忆及相关 Markdown 台账/计划文档时，一律显式使用 **UTF-8** 编码；不要依赖 PowerShell / 编辑器 / 系统默认代码页，避免中文内容写入后出现乱码。

## 代码注释（强制）

- **所有代码必须编写必要的说明注释，每个函数都必须有自己的注释**。此要求适用于前端、后端、脚本及测试代码，包含普通函数、方法、构造函数、箭头函数、匿名函数和回调函数；不得因函数简短或命名清晰而省略。
- 函数注释应紧邻函数定义，说明其职责与用途；涉及重要参数、返回值、边界条件、副作用或错误处理约定时，应一并说明。匿名函数和内联回调也必须在对应定义前或函数体开头写明自身用途，不能用外层函数的注释代替。
- 注释优先使用中文，遵循对应语言的文档注释惯例（如 TypeScript 的 JSDoc、Go 以函数名开头的说明注释）；复杂分支、业务约束及不直观的实现还须补充局部注释，解释原因，避免只重复代码字面含义。
- 新增或修改代码时必须同步补齐相关函数的注释，并随实现更新，确保注释与实际行为一致；提交前检查本次涉及的函数均有独立、有效的注释。
