---
description: Minimal always-on repo identity and rule handoff
alwaysApply: true
---

# Project Standards

- This repo is currently a `Vue 3 + TypeScript + Vite` project using `shadcn-vue`.
- Treat current implementation facts and future desktop architecture as separate layers.
- Use the focused rules in `project-facts.mdc`, `architecture-boundaries.mdc`, and `vue-frontend-standards.mdc` as the primary guidance for edits.
