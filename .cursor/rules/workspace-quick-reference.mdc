---
description: 日常开发速记（启动、联调、根目录政策）
alwaysApply: true
---

# Workspace Quick Reference（项目记忆）

## 一眼记住

- **产品正式名称**：`Curated`。仓库目录 / npm `name` 仍可能是 `jav-shadcn`。
- **前端启动**：仓库根目录运行 `pnpm dev`，默认 `http://localhost:5173`。
- **后端启动**：`cd backend && go run ./cmd/curated`，开发默认地址 `127.0.0.1:8080`（仅 loopback），健康名 `curated-dev`。
- **开发态 Windows 后端二进制**：`pnpm backend:build:dev`，产物必须是 `backend/runtime/curated-dev.exe`，不要生成开发态 `curated.exe`。
- **Electron MVP 桌面壳层**：`pnpm dev:electron` 会先构建 `backend/runtime/curated-dev.exe` 与 `electron-dist/`，再启动 Electron；开发态会启动或复用 Go 后端 `http://127.0.0.1:8080` 与 Vite 前端 `http://127.0.0.1:5173`，主窗口和托盘 Web 入口加载 `5173`；生产安装包的顶层 `Curated.exe` 是 Electron 壳，内置 Go 后端在 `resources/app/curated.exe`，打包态加载后端静态托管 `http://127.0.0.1:8081`。使用 Curated 图标和托盘；关闭窗口会隐藏到托盘，托盘菜单可恢复窗口 / 浏览器打开 Web 端 / 打开 Settings / 真正退出；preload 暴露 `window.javLibrary.pickDirectory()` 用于原生目录选择，以及只读 `windowChrome`（`macos` / `native`）窗口样式能力。macOS 使用融合标题栏与原生红黄绿按钮。`build:electron:main` 同步生成本地 Desktop 版本元数据；关于页通过 `getDesktopInfo()` 显示 `0.1.0` 与独立开发版标识，通过 `checkDesktopUpdate()` 检查本机状态；当前一体包仍走原安装器更新。
- **release 构建默认地址**：`-tags release` 时默认 `127.0.0.1:8081`（仅 loopback），健康名 `curated`。设置页「网络」可把 `lanEnabled` 写入 `library-config.cfg`（不要求 PIN）；完全退出后重新打开会把同端口改绑到 `0.0.0.0`。主运行时 JSON 的非 loopback `httpAddr` 仍必须 `lanEnabled: true`，否则拒绝启动 HTTP 服务。
- **Mock / Web API 切换**：根目录 `.env` 设置 `VITE_USE_WEB_API=true` 时走真实 API；否则走 Mock。
- **Vite 代理 / 直连后端**：`vite.config.ts` 将 `/api` 代理到 `http://127.0.0.1:8080`（长上传关闭 proxy 超时）；仅本地 loopback Web API 开发态下，`src/api/http-client.ts` 和播放器媒体流会直连开发后端 `:8080`，避免大文件上传/大视频流经过 Vite proxy；release `:8081` 静态托管继续走同源 `/api`。
- **本地静态托管**：存在 `frontend-dist/` 或 `dist/` 时，后端会直接托管前端静态文件并对非 `/api` 路由做 SPA fallback；`index.html` 与 SPA fallback 强制 `no-store`，`/assets/*` 内容哈希资源使用 immutable 长缓存，其余静态文件要求重新验证，避免 Electron 升级后继续命中旧入口。

## 根目录政策（阶段 1）

- **主要 tracked 根目录**：`src/`、`backend/`、`docs/`、`scripts/`、`public/`、`icon/`、`.cursor/`
- **GitHub 跟踪边界（2026-09-25）**：保留全部文档和原型、源码、测试、依赖锁、运行必需字体与许可证；`backend/frontend-dist/`、`.claude/settings.local.json`、`.agents/` 只留本机。CI 用 `git ls-files -ci --exclude-standard` 拦截被忽略却仍被跟踪的文件；取消跟踪用 `git rm --cached` 保留本地副本，不清除历史提交。详见 `docs/guide.md` 的 Repository tracking policy。
- **固定保留在根目录的本地目录**：
  - `videos_test/`：测试素材目录，保留原位，不纳入目录重排
  - `config/`：资料库级运行配置目录，暂时继续保留在 repo root，不并入 `backend/internal/config`
- **允许的开发态运行产物**：`backend/runtime/`，用于开发二进制、数据库、日志等
- **Electron 编译产物**：`electron-dist/` 为本地构建输出，不纳入源码跟踪
- **新的本地临时目录优先放到**：`.workspace/`
- **不要**继续随意增加新的根目录临时目录
- **禁止**在仓库内主动创建 Go 构建缓存目录：`.gocache/`、`.tmp-go/`、`.tmp-go-modcache/`
- **release 构建现状**：`scripts/release/release_cli.py` / `scripts/release/release_lib/build_steps.py` 已切到系统临时目录承载 Go build cache，不再写 repo 内缓存目录；`pnpm release:publish` 会构建前端、release Go 后端和 Electron main process，并组装 Electron runtime 为 `release/Curated/Curated.exe`；安装器覆盖升级前会删除 `{app}/frontend-dist` 与 `{app}/resources/app/frontend-dist` 两处受管理前端产物，再复制新构建，避免旧哈希资源残留；打包会将真实 FFmpeg 放入 `resources/app/third_party/ffmpeg/bin/`，拒绝复制 Scoop shim，找不到真实运行时时失败。
- **Windows / Mac Desktop CD**：`cd-release.yml` 接受 `full-vX.Y.Z` / `server-vX.Y.Z` / `desktop-vX.Y.Z`，构建对应组件；Full 含 Windows 三包和 Apple Silicon Desktop。旧 v-tag 流程保留在 `cd-legacy.yml`。手动默认 draft，CI/安装验收/摘要校验均通过后才能公开，已公开版本不覆盖。详见 `docs/guide.md` §8。
- **生产包保留规则**：未得到用户明确要求时，禁止删除已经打出的生产包产物；`release/installer/*.exe` 与 `release/portable/*.zip` 都必须保留，即使准备重新打包或同版本重打，也不能主动清理既有 installer / portable 包。

## Server 版本

health 的 `version` 为数字 SemVer（当前 1.5.8），`buildStamp` 单独保留构建时间戳；关于页分开展示。Server 源为 `backend/internal/version/server.json`，Desktop 源为 `scripts/release/versions/desktop.json`（0.1.0）。

## 配置与持久化

- **资料库级 JSON 配置文件**：`config/library-config.cfg`
- 该文件保存：
  - `organizeLibrary`
  - `metadataMovieProvider`
  - `metadataMovieStrategy`
  - `autoLibraryWatch`
  - `autoActorProfileScrape`
  - `autoDownloadUpdates`
  - `launchAtLogin`
  - `browserPluginEnabled`（设置 → 网络与设备 → 浏览器插件联动，默认关闭，保存立即生效，无需凭证）
  - `lanEnabled`（设置 → 网络与设备；不要求 PIN，改监听要完全退出）
  - `backupDirectory`
  - `proxy`
  - `aiProvider` / `aiGovernance`（正式 Settings → AI；缺省禁用，治理经 `/api/ai/settings` 保存）
  - `logDir` / `logFilePrefix` / `logMaxAgeDays` / `logLevel`
- `PATCH /api/settings` 会部分更新这些资料库级设置。
- 空 `logDir` 表示使用默认日志目录，而不是关闭文件日志：
  - release 默认 `LOCALAPPDATA\\Curated\\logs`
  - dev 默认 `backend/runtime/logs`

## Agent 作品回答（2026-09-09）

- **2026-09-21 上下文连续性**：新版后端启动自动迁移 0052，旧对话首次续聊按需生成持久摘要；长任务自动整理工作上下文后接续。前端显示整理/已整理/部分记忆缺失状态。需要 Web API 模式；配置模型新增上下文窗口（默认 65536 tokens，范围 32768–2097152）与官方容量预设，保存到 aiProvider.contextWindow，下一次聊天生效。摘要请求会调用现有模型并计入用量。不可压缩的大输入仍需缩短，但可继续原会话。详见 `docs/plan/2026-09-21-agent-context-continuity.md`。

- 当前 Agent 聊天使用本轮作品引用与服务端事实填充；正文通过校验后展示，过程显示“正在整理结果”，原始思考不再流出。无依据的作品草稿最多修正一次，再失败返回部分完成。
- SSE 每 15 秒心跳；每次模型调用 2 分钟。工具步数仍遵循设置（0 表示不限）。历史保存已发布正文与来源快照，不保存未校验草稿、不授予旧引用权限。
- 无新增配置或数据库迁移；后端重新运行新代码后生效。独立 Action 与来源元数据准确性不在此保证内。详见 `docs/guide.md` 与 milestone plan §16.6。

## 构建体积监管（2026-09-25）

- `pnpm build` 按 `bundle-policy.json` 执行分级监管：正常增长放行、关注/突增/累计增长提醒、绝对严重超限才失败。旧逐 chunk 硬预算退役。
- 报告：`dist/bundle-report.md` / `bundle-analysis.json`，CI Summary 与 Artifact 留存 90 天；范围仅前端，不含 Electron/Go/FFmpeg/安装包。
- 主分支成功前端质量作业缓存用于 20% 单次增长比较，人工 `bundle-baseline.json` 用于 50% 累计增长比较，两者均有绝对字节噪声门槛。基线/缓存缺失或模式不同不关闭绝对限制。
- `pnpm bundle:baseline` 仅在重新执行 Web API 生产构建并审阅后手动更新、提交；禁止每次构建自动更新或自动上调阈值。完整规则见 `docs/ops/2026-04-08-agent-build-and-test.md`。

## Desktop 服务器连接

设置 → 网络与设备顶部显示当前服务器、连接状态和保存列表，添加使用页面内弹框填写名称/地址，保存后刷新列表；管理进入本地窗口；托盘「服务器」也可打开；服务器名称、HTTP/HTTPS 根地址及上次成功连接保存到 userData/servers.json，按 origin（含端口）隔离登录会话。切换先探测并加载新页面，失败保留旧页面，成功后关闭旧页面并提示播放/上传/未保存编辑影响。升级隔离会话后可能需重新输入 PIN。legacy 默认本机连接保留 Go/Vite 启动；远端连接失败不拉起本机后端。旧 Server UI 没有设置列表时仍可用托盘。详情见 guide 的 Desktop server connections。

## 常见操作

- **重启前后端**：先释放 `5173` 与后端端口（开发 `8080` / release `8081` 或配置里的 `httpAddr`），再分别启动。
- **测试托盘模式**：Windows 下可运行 `go run ./cmd/curated -mode tray`；二次启动会复用现有实例并重新打开浏览器。
- **修改 API 行为**：优先改 `backend/internal/server` 与 `backend/internal/app`，前端路径集中在 `src/api/endpoints.ts`。
- **离线路径迁移**：完全退出 Curated 后在 `backend/` 先运行 `go run ./cmd/curated -maintenance path-migrate-plan -path-from <absolute> -path-to <absolute>`；apply 额外要求新 `-backup-path` 与 `-confirm-path-migration`。自定义数据库路径同时传 `-config <path>`；不要删除 `.runtime.lock` 绕过运行时互斥。
- **修改 README / 对外说明**：如果用户可见行为、命令入口、目录政策或公开说明变了，检查 `README.md`、`README.zh-CN.md`、`README.ja-JP.md` 是否需要同步；操作细节、配置长文和文档索引改 **`docs/guide.md`**，不要把长文写回根 README。
- **打包历史**：`pnpm release:portable`、`pnpm release:installer`、`pnpm release:publish` 会向 `docs/ops/package-build-history.csv` 追加记录。

## 规则索引

- 实现细节与目录事实：`project-facts.mdc`
- 前后端分层与桌面目标边界：`architecture-boundaries.mdc`
- UI 分层与设计令牌：`ui-component-spec.mdc`
- Vue 前端约束：`vue-frontend-standards.mdc`、`jav-library-frontend-patterns.mdc`
- Go / 合约 / 文档同步：`backend-go-standards.mdc`、`backend-api-contracts.mdc`、`docs-sync.mdc`

## 漫画 / 写真 Beta（2026-09-10）

真实试用需 `VITE_USE_WEB_API=true` 并运行新版后端。进入「设置 → 资料库 → 漫画库 / 写真库」，分别启用对应 Beta，再添加该库的绝对路径并通过路径「更多操作」扫描 ZIP/CBZ。关闭只隐藏内容入口和配置、停止该库监听，保留已有数据；设置入口仍可用于重新启用。Mock 开关仅当前会话有效。

## 远程设置管理（2026-09-27）

远程设置仅保留客户端偏好和只读信息；全局配置、安全、AI 管理及备份维护须在 Server 本机操作，接口同步限制，远程不返回 AI/代理凭据。详见 `docs/guide.md` 的 Remote settings access。 新后端重启、客户端刷新后生效；`PATCH /api/settings` 远程统一返回 `403 SERVER_SETTINGS_READ_ONLY`，当前会话解锁/锁定及上传等业务操作保留。

- **Mac Desktop 本地打包**：Apple Silicon 主机执行 `pnpm release:macos-desktop`；仅客户端，版本 desktop.json，ad-hoc 签名，输出 release/macos-desktop；同版本文件拒绝覆盖。


## Split release implementation (2026-09-27)

Windows Full/Server/Desktop installers and ZIPs are now implemented in `component_cd.py` / `windows_components.py`; Apple Silicon Desktop DMG/ZIP shares the Desktop version. Full embeds the exact standalone installers, uses their component identities, preserves newer installations and reports partial failures. Server runs independently in the current-user tray; Desktop only connects. Component releases use `full-v` / `server-v` / `desktop-v` tags and isolated stable manifests on `release-channels`; legacy `/releases/latest` stays on the compatible all-in-one package. Server rejects legacy installer cache; local Desktop connections expose independent Desktop update checks. Initial versions: Server 1.6.0, Desktop 0.1.0, Full 1.6.0. Old-install detection requires the documented backup/manual migration; automated custom/account migration remains pending; SSDP is implemented in 1.7.0. Windows real installer/lifecycle smoke checks gate CD. See guide §8 for exact names, update isolation and recovery.


## 2026-09-27 worktree integration

Desktop 0.2.0 uses the bundled Vue launcher and starts independently (`pnpm dev:electron` no longer starts Go/Vite). Server 1.7.0 adds public `/api/server-info`, persistent `<databasePath>.server-id`, optional IPv4 SSDP and `/discovery/description.xml`. `discoveryEnabled` requires LAN listening; changes apply after restart. Released `servers.json` and origin sessions are preserved; development `connections.json` imports atomically only when no released profile exists. Canonical CD/names/version/update isolation remains authoritative; package.yml delegates to it in draft mode. Upgrade details and limitations: docs/guide.md, “Desktop discovery and upgrade”. These facts supersede earlier development/pending notes above.


## Full 1.7.2 legacy upgrade implementation (2026-09-27)

Source now targets Full/Server 1.7.2 (Desktop 0.2.0 unchanged). Full embeds `cmd/curated-migrate`: same-user legacy detection, verified offline backup under `%LOCALAPPDATA%\Curated\installer-migrations`, internal old uninstall (UAC only for machine-wide removal), independent component install and retry journal. Standalone Server reads `server-startup.json` to preserve the original external data root/custom config on normal and login startup. No new HTTP API or library-config keys. Registered 1.x all-in-one versions below 1.6 are recognized; real 1.4.13/1.5.8 CD baselines added. Running processes, ambiguous installations, relative/program-local data and linked program trees are blocked before removal. Users must quit from tray; no forced termination or cross-account migration. Saved connections copy only if the new profile is empty; authentication sessions are not merged. Local tests/cross-compilation passed; Windows Inno/UAC/install gates pending. Published 1.7.1 remains unchanged; see guide “Migrating an old all-in-one installation”. This supersedes earlier manual-only statements for the new source, not historical packages.
