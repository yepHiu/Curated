---
description: 日常开发速记（启动、联调、根目录政策）
alwaysApply: true
---

# Workspace Quick Reference（项目记忆）

## 一眼记住

- **产品正式名称**：`Curated`。仓库目录 / npm `name` 仍可能是 `jav-shadcn`。
- **前端启动**：仓库根目录运行 `pnpm dev`，默认 `http://localhost:5173`。
- **后端启动**：`cd backend && go run ./cmd/curated`，开发默认地址 `127.0.0.1:8080`（仅 loopback），健康名 `curated-dev`。
- **开发态 Windows 后端二进制**：`pnpm backend:build:dev`，产物必须是 `backend/runtime/curated-dev.exe`，不要生成开发态 `curated.exe`。
- **Electron MVP 桌面壳层**：`pnpm dev:electron` 会先构建 `backend/runtime/curated-dev.exe` 与 `electron-dist/`，再启动 Electron；开发态会启动或复用 Go 后端 `http://127.0.0.1:8080` 与 Vite 前端 `http://127.0.0.1:5173`，主窗口和托盘 Web 入口加载 `5173`；生产安装包的顶层 `Curated.exe` 是 Electron 壳，内置 Go 后端在 `resources/app/curated.exe`，打包态加载后端静态托管 `http://127.0.0.1:8081`。使用 Curated 图标和托盘；关闭窗口会隐藏到托盘，托盘菜单可恢复窗口 / 浏览器打开 Web 端 / 打开 Settings / 真正退出；preload 仅暴露 `window.javLibrary.pickDirectory()` 用于原生目录选择。
- **release 构建默认地址**：`-tags release` 时默认 `127.0.0.1:8081`（仅 loopback），健康名 `curated`。设置页「网络」可把 `lanEnabled` 写入 `library-config.cfg`（不要求 PIN）；完全退出后重新打开会把同端口改绑到 `0.0.0.0`。主运行时 JSON 的非 loopback `httpAddr` 仍必须 `lanEnabled: true`，否则拒绝启动 HTTP 服务。
- **Mock / Web API 切换**：根目录 `.env` 设置 `VITE_USE_WEB_API=true` 时走真实 API；否则走 Mock。
- **Vite 代理 / 直连后端**：`vite.config.ts` 将 `/api` 代理到 `http://127.0.0.1:8080`（长上传关闭 proxy 超时）；仅本地 loopback Web API 开发态下，`src/api/http-client.ts` 和播放器媒体流会直连开发后端 `:8080`，避免大文件上传/大视频流经过 Vite proxy；release `:8081` 静态托管继续走同源 `/api`。
- **本地静态托管**：存在 `frontend-dist/` 或 `dist/` 时，后端会直接托管前端静态文件并对非 `/api` 路由做 SPA fallback；`index.html` 与 SPA fallback 强制 `no-store`，`/assets/*` 内容哈希资源使用 immutable 长缓存，其余静态文件要求重新验证，避免 Electron 升级后继续命中旧入口。

## 根目录政策（阶段 1）

- **主要 tracked 根目录**：`src/`、`backend/`、`docs/`、`scripts/`、`public/`、`icon/`、`.cursor/`
- **GitHub 跟踪边界（2026-09-25）**：保留全部文档和原型、源码、测试、依赖锁、运行必需字体与许可证；`backend/frontend-dist/`、`.claude/settings.local.json`、`.agents/` 只留本机。CI 用 `git ls-files -ci --exclude-standard` 拦截被忽略却仍被跟踪的文件；取消跟踪用 `git rm --cached` 保留本地副本，不清除历史提交。详见 `docs/guide.md` 的 Repository tracking policy。
- **固定保留在根目录的本地目录**：
  - `videos_test/`：测试素材目录，保留原位，不纳入目录重排
  - `config/`：资料库级运行配置目录，暂时继续保留在 repo root，不并入 `backend/internal/config`
- **允许的开发态运行产物**：`backend/runtime/`，用于开发二进制、数据库、日志等
- **Electron 编译产物**：`electron-dist/` 为本地构建输出，不纳入源码跟踪
- **新的本地临时目录优先放到**：`.workspace/`
- **不要**继续随意增加新的根目录临时目录
- **禁止**在仓库内主动创建 Go 构建缓存目录：`.gocache/`、`.tmp-go/`、`.tmp-go-modcache/`
- **release 构建现状**：`scripts/release/release_cli.py` / `scripts/release/release_lib/build_steps.py` 已切到系统临时目录承载 Go build cache，不再写 repo 内缓存目录；`pnpm release:publish` 会构建前端、release Go 后端和 Electron main process，并组装 Electron runtime 为 `release/Curated/Curated.exe`；安装器覆盖升级前会删除 `{app}/frontend-dist` 与 `{app}/resources/app/frontend-dist` 两处受管理前端产物，再复制新构建，避免旧哈希资源残留；打包会将真实 FFmpeg 放入 `resources/app/third_party/ffmpeg/bin/`，拒绝复制 Scoop shim，找不到真实运行时时失败。
- **生产包保留规则**：未得到用户明确要求时，禁止删除已经打出的生产包产物；`release/installer/*.exe` 与 `release/portable/*.zip` 都必须保留，即使准备重新打包或同版本重打，也不能主动清理既有 installer / portable 包。

## 配置与持久化

- **资料库级 JSON 配置文件**：`config/library-config.cfg`
- 该文件保存：
  - `organizeLibrary`
  - `metadataMovieProvider`
  - `metadataMovieStrategy`
  - `autoLibraryWatch`
  - `autoActorProfileScrape`
  - `autoDownloadUpdates`
  - `launchAtLogin`
  - `browserPluginEnabled`（设置 → 网络与设备 → 浏览器插件联动，默认关闭，保存立即生效，无需凭证）
  - `lanEnabled`（设置 → 网络与设备；不要求 PIN，改监听要完全退出）
  - `backupDirectory`
  - `proxy`
  - `aiProvider` / `aiGovernance`（正式 Settings → AI；缺省禁用，治理经 `/api/ai/settings` 保存）
  - `logDir` / `logFilePrefix` / `logMaxAgeDays` / `logLevel`
- `PATCH /api/settings` 会部分更新这些资料库级设置。
- 空 `logDir` 表示使用默认日志目录，而不是关闭文件日志：
  - release 默认 `LOCALAPPDATA\\Curated\\logs`
  - dev 默认 `backend/runtime/logs`

## Agent 作品回答（2026-09-09）

- **2026-09-21 上下文连续性**：新版后端启动自动迁移 0052，旧对话首次续聊按需生成持久摘要；长任务自动整理工作上下文后接续。前端显示整理/已整理/部分记忆缺失状态。需要 Web API 模式；配置模型新增上下文窗口（默认 65536 tokens，范围 32768–2097152）与官方容量预设，保存到 aiProvider.contextWindow，下一次聊天生效。摘要请求会调用现有模型并计入用量。不可压缩的大输入仍需缩短，但可继续原会话。详见 `docs/plan/2026-09-21-agent-context-continuity.md`。

- 当前 Agent 聊天使用本轮作品引用与服务端事实填充；正文通过校验后展示，过程显示“正在整理结果”，原始思考不再流出。无依据的作品草稿最多修正一次，再失败返回部分完成。
- SSE 每 15 秒心跳；每次模型调用 2 分钟。工具步数仍遵循设置（0 表示不限）。历史保存已发布正文与来源快照，不保存未校验草稿、不授予旧引用权限。
- 无新增配置或数据库迁移；后端重新运行新代码后生效。独立 Action 与来源元数据准确性不在此保证内。详见 `docs/guide.md` 与 milestone plan §16.6。

## 常见操作

- **重启前后端**：先释放 `5173` 与后端端口（开发 `8080` / release `8081` 或配置里的 `httpAddr`），再分别启动。
- **测试托盘模式**：Windows 下可运行 `go run ./cmd/curated -mode tray`；二次启动会复用现有实例并重新打开浏览器。
- **修改 API 行为**：优先改 `backend/internal/server` 与 `backend/internal/app`，前端路径集中在 `src/api/endpoints.ts`。
- **离线路径迁移**：完全退出 Curated 后在 `backend/` 先运行 `go run ./cmd/curated -maintenance path-migrate-plan -path-from <absolute> -path-to <absolute>`；apply 额外要求新 `-backup-path` 与 `-confirm-path-migration`。自定义数据库路径同时传 `-config <path>`；不要删除 `.runtime.lock` 绕过运行时互斥。
- **修改 README / 对外说明**：如果用户可见行为、命令入口、目录政策或公开说明变了，检查 `README.md`、`README.zh-CN.md`、`README.ja-JP.md` 是否需要同步；操作细节、配置长文和文档索引改 **`docs/guide.md`**，不要把长文写回根 README。
- **打包历史**：`pnpm release:portable`、`pnpm release:installer`、`pnpm release:publish` 会向 `docs/ops/package-build-history.csv` 追加记录。

## 规则索引

- 实现细节与目录事实：`project-facts.mdc`
- 前后端分层与桌面目标边界：`architecture-boundaries.mdc`
- UI 分层与设计令牌：`ui-component-spec.mdc`
- Vue 前端约束：`vue-frontend-standards.mdc`、`jav-library-frontend-patterns.mdc`
- Go / 合约 / 文档同步：`backend-go-standards.mdc`、`backend-api-contracts.mdc`、`docs-sync.mdc`

## 漫画 / 写真 Beta（2026-09-10）

真实试用需 `VITE_USE_WEB_API=true` 并运行新版后端。进入「设置 → 资料库 → 漫画库 / 写真库」，分别启用对应 Beta，再添加该库的绝对路径并通过路径「更多操作」扫描 ZIP/CBZ。关闭只隐藏内容入口和配置、停止该库监听，保留已有数据；设置入口仍可用于重新启用。Mock 开关仅当前会话有效。
