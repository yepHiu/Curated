---
description: Git 以最小修改单元提交；仅用户明确要求时才推送远程
alwaysApply: true
---

# Git 提交与推送（代理必遵）

## 最小修改单元（atomic commits）

- **一次提交只承载一个可独立说明的变更**：单一缺陷修复、单一小功能切片、单一重构意图、或一组强耦合且必须同批落地的改动（例如合约 + 唯一调用方）。
- **不要**把彼此无关的修改塞进同一 commit（例如：后端 API 调整 + 顺手改文案 + 无关格式化 + 另一功能的半套改动）。
- **可拆则拆**：文档 / 规则 / 计划与业务代码尽量分提交；测试与实现可同提交若属于同一行为变更。
- **提交前 staging 要有意识**：用 `git add -p` 或按文件/目录挑选，确保暂存区与提交说明一致。
- **提交说明**用简短主题行写清「这一最小单元」做了什么；需要时用正文列要点。

## 对话中的执行方式

- **凡是本轮对话中改动了代码（含配置、迁移、脚本）**：在合适节点按 **最小修改单元** 执行 `git commit`，而不是攒一大包再交一次。
- 若用户一次诉求内含多个独立单元：完成一个单元就提交一次，再开始下一单元。
- **禁止**在用户未明确要求时执行 **`git push`**（含 `push origin`、PR 同步推送等）。仅当用户明确说「推送到仓库 / push / 推送远程」等时再推送。

## 与「未要求提交」的边界

- 若用户明确说「先不要提交 / 只改代码」：以用户当轮指示为准。
- 工作区仅有探索性、未定型修改时：可先不提交，但在收尾或用户确认后再按单元拆分提交。
