---
description: 仓库当前实现事实（前后端、目录、契约）
alwaysApply: true
---

# Project Facts（当前实现）

- **产品正式名称：Curated**（仓库目录 / npm `name` 等可能仍为 `jav-shadcn`）。

## 技术栈与包管理

- 根目录：`Vue 3 + TypeScript + Vite`，UI 为 `shadcn-vue` + Tailwind CSS v4，主题变量在 `src/style.css`。
- 包管理器：**pnpm**；以 `pnpm-lock.yaml` 为准。
- 路径别名：`@` → `src`。

## 后端（已实现）

- Go 服务在 **`backend/`**，HTTP 入口：`cd backend && go run ./cmd/javd`；默认 **`httpAddr` `:8080`**（见 `backend/internal/config`）。
- **SQLite** 应用库、扫描结果、任务等与 `config` 中的 `databasePath` 绑定；刮削侧另有 Metatube 引擎相关 SQLite（日志里可见 GORM）。
- 主要逻辑：`backend/internal/app`（扫描、刮削、任务）、`server`（HTTP）、`storage`、`scanner`、`scraper`、`tasks`。
- HTTP 路由前缀 **`/api`**（节选）：`health`、`library/movies`（列表/详情/删除）、**`GET library/played-movies`**、**`POST library/played-movies/{movieId}`**（已播放去重统计，表 `library_played_movies`）、**`GET library/movies/{movieId}/stream`**（主视频流，支持 Range）、**`POST library/movies/{movieId}/scrape`**（单部刷新元数据）、`library/paths`、`settings`、`scans`、`tasks/{taskId}`、**`playback/progress`**（列表/按片 PUT/DELETE）、**`curated-frames`**（列表/POST、按 id 图/标签 PATCH/DELETE）。

## 前端结构

- 入口：`src/main.ts`（Web API 时并行 **`hydratePlaybackProgress`**、**`hydratePlayedMovies`** 再挂载）；`App.vue` 仅挂路由；主壳 **`src/layouts/AppShell.vue`**。
- 路由含：`library`、`favorites`、`recent`、`tags`、**`history`（观看历史）**、`detail`、`player`、`settings` 等（见 `src/router/index.ts`）。
- 业务展示组件：**`src/components/jav-library`**；通用 UI：**`src/components/ui`**。
- **领域类型**：`src/domain/**`；与后端对齐的 DTO/请求类型：**`src/api/types.ts`**；HTTP 封装：**`src/api/http-client.ts`**、**`src/api/endpoints.ts`**。
- **服务层**：契约 **`src/services/contracts/library-service.ts`**；实现 **`src/services/adapters/web/web-library-service.ts`**（真实 API）与 **`mock/mock-library-service.ts`**（本地演示）。
- 切换方式：环境变量 **`VITE_USE_WEB_API`**（如根目录 `.env` 中 `true`）→ Web 适配器；否则 Mock。
- 长任务（扫描、单部刮削）：**`src/composables/use-scan-task-tracker.ts`** 轮询 **`GET /api/tasks/{taskId}`**；详情页在刮削完成后会 **`loadMovieDetail`** 刷新。
- **观看进度 / 历史**：`src/lib/playback-progress-storage.ts`；**`VITE_USE_WEB_API=true`** 时启动 **`hydratePlaybackProgress()`**（`main.ts`）并从 **`GET/PUT/DELETE /api/playback/progress`** 读写服务端 SQLite；Mock 时用 **`localStorage`**（`jav-library-playback-progress-v1`）。`player-route`、`playback-history-groups`；**`HistoryView`** + **`PlaybackHistoryCard`**。
- **萃取帧**：**Web API** 时 **`POST/GET/PATCH/DELETE /api/curated-frames`**，图像 **`GET /api/curated-frames/{id}/image`**（`src/lib/curated-frame-image-url.ts`）；Mock 时仍为 **IndexedDB**（`src/lib/curated-frames/db.ts`）。
- 播放器续播：`PlayerPage` 读 `?t=` 或本地进度 seek，节流写入进度；`AppShell` 在 `player` 且 `query.from=history` 时返回历史。
- 开发时代理：`vite.config.ts` 将 **`/api` 代理到 `http://localhost:8080`**；前端请求基址默认为 **`/api`**（见 `http-client`），一般无需再配 `VITE_API_BASE_URL`。
- `components.json` 为 **shadcn-vue**  schema，勿按 React shadcn 目录假设。

## 其它约束

- 已使用 **`vue-router`** 与 **`vue-virtual-scroller`**；大图海报列表勿退回非虚拟化或整页无路由切换。
- 原型阶段仍优先：**可复用边界、类型契约、服务层**，避免一次性页面逻辑堆叠。
