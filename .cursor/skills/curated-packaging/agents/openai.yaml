name: curated-packaging
model: gpt-4o-mini
version: "0.3.0"
description: |
  Use this repo-local packaging skill for natural-language Curated release requests. Preview with the Python helper first, including the active CSV package-history ledger state, then execute through scripts/release/release_cli.py.
