name: curated-packaging
model: gpt-4o-mini
version: "0.4.0"
description: |
  Use this repo-local packaging skill for natural-language Curated release requests. Preview with the Python helper first, including the active CSV package-history ledger state, then execute through scripts/release/release_cli.py. After every successful production packaging run, also write a release note to docs/release-notes/ and return a copyable release draft to the user.
